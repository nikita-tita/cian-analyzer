# 🚀 Быстрый Гайд по Деплою

## TL;DR - Минимальная команда

```bash
# На production сервере:
cd /var/www/housler  # или /root/cian-analyzer
./DEPLOY_TO_PRODUCTION.sh
```

---

## Полная Инструкция

### 1. Подключение к серверу

```bash
# Через SSH
ssh root@YOUR_SERVER_IP
# или
ssh root@housler.ru
```

### 2. Навигация к проекту

```bash
# Обычно проект в одной из директорий:
cd /var/www/housler
# или
cd /root/cian-analyzer
# или
cd /opt/housler
```

### 3. Запуск деплоя

```bash
# Подтянуть последние изменения
git pull origin main

# Запустить деплой
./DEPLOY_TO_PRODUCTION.sh
```

**Скрипт автоматически:**
- ✅ Создаст бэкап БД
- ✅ Подтянет изменения из main
- ✅ Обновит зависимости
- ✅ Перезапустит сервис
- ✅ Проверит здоровье приложения

---

## Что Изменилось

### Коммит: faf15c7

1. **Исправлена фильтрация по региону** ⚠️ КРИТИЧНО
2. **Исправлены тесты** (175/177 passing)
3. **Добавлена документация**

---

## Проверка После Деплоя

```bash
# 1. Health check
curl https://housler.ru/health

# 2. Логи
sudo journalctl -u housler -f

# 3. Тест парсинга
curl -X POST https://housler.ru/api/parse \
  -H 'Content-Type: application/json' \
  -d '{"url":"https://www.cian.ru/sale/flat/322762697/"}'
```

---

## Откат

```bash
git revert HEAD
sudo systemctl restart housler
```

