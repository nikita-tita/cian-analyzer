# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ ПРОДА - Housler.ru

**Дата:** 23 ноября 2025
**Статус:** 🔴 **Сайт НЕ РАБОТАЕТ - HTTP 403 на всех endpoints**

---

## 📊 Текущее состояние

### ❌ Что НЕ работает:

```bash
curl https://housler.ru/          # → 403 Forbidden
curl https://housler.ru/health    # → 403 Forbidden
curl https://housler.ru/calculator # → 403 Forbidden
curl https://housler.ru/blog      # → 403 Forbidden
```

**Все запросы возвращают:** `"Access denied"` от `server: envoy`

### ✅ Что работает:

- DNS резолвится → IP адрес получается
- Envoy proxy отвечает → сервер доступен
- Последний деплой кода → был успешен (блог добавлен)

---

## 🔍 ДИАГНОСТИКА: Где проблема

### 1. Это НЕ проблема кода

**Доказательства:**
- Блокируются ВСЕ endpoints (включая старые `/health`, `/calculator`)
- Ответ приходит от `envoy` прокси, а не от вашего приложения
- Запросы НЕ ДОХОДЯТ до сервера 91.229.8.221

### 2. Это проблема Yandex Cloud Load Balancer

**Признаки:**
- Ответ от `server: envoy` → это Yandex Cloud Application Load Balancer
- HTTP 403 → блокировка на уровне балансера
- Одинаковый ответ для всех запросов → WAF или Security Group

---

## 🛠️ КАК ИСПРАВИТЬ (3 сценария)

### Сценарий 1: Security Group блокирует трафик (НАИБОЛЕЕ ВЕРОЯТНО)

#### Шаги исправления:

1. **Откройте Yandex Cloud Console**
   ```
   https://console.cloud.yandex.ru/
   ```

2. **Перейдите в Application Load Balancer**
   ```
   Меню → Application Load Balancer → Найдите ваш балансер
   ```

3. **Проверьте Security Groups**
   ```
   Кликните на балансер → Network Settings → Security Groups
   ```

4. **Проверьте Ingress Rules (входящий трафик)**

   Должны быть правила:
   ```
   ✓ Protocol: TCP
   ✓ Port: 80
   ✓ Source: 0.0.0.0/0
   ✓ Action: ALLOW

   ✓ Protocol: TCP
   ✓ Port: 443
   ✓ Source: 0.0.0.0/0
   ✓ Action: ALLOW
   ```

5. **Если правил нет - ДОБАВЬТЕ:**
   ```
   Security Groups → Ваша группа → Edit Rules

   Ingress Rules:
   - Add Rule
   - Protocol: TCP
   - Port: 80
   - CIDR: 0.0.0.0/0
   - Save

   - Add Rule
   - Protocol: TCP
   - Port: 443
   - CIDR: 0.0.0.0/0
   - Save
   ```

6. **Подождите 1-2 минуты и проверьте:**
   ```bash
   curl https://housler.ru/health
   ```

---

### Сценарий 2: Backend Group видит сервер как Unhealthy

#### Шаги исправления:

1. **Откройте Backend Groups**
   ```
   Yandex Cloud Console → Application Load Balancer → Backend Groups
   ```

2. **Найдите вашу группу → кликните на неё**

3. **Проверьте статус Targets:**
   ```
   Target: 91.229.8.221
   Status: ??? (должен быть зеленый - Healthy)
   ```

4. **Если Status = Unhealthy (красный):**

   **Причина:** Балансер не может достучаться до сервера на порт 80

   **Решение A - Проверить Security Group сервера:**
   ```
   VPC → Security Groups → Группа сервера (91.229.8.221)

   Должно быть Ingress правило:
   - Protocol: TCP
   - Port: 80
   - Source: <IP подсети балансера или 0.0.0.0/0>
   - Action: ALLOW
   ```

   **Решение B - Проверить Health Check настройки:**
   ```
   Backend Groups → Ваша группа → Health Check Settings

   Должно быть:
   - Type: HTTP
   - Path: /health
   - Port: 80
   - Interval: 10
   - Timeout: 5
   - Healthy threshold: 2
   - Unhealthy threshold: 2
   ```

5. **Если изменили настройки - подождите 20-30 секунд**

6. **Проверьте статус снова - должен стать зеленым**

---

### Сценарий 3: HTTP Router неправильно настроен

#### Шаги исправления:

1. **Откройте HTTP Routers**
   ```
   Yandex Cloud Console → Application Load Balancer → HTTP Routers
   ```

2. **Найдите ваш роутер → кликните**

3. **Проверьте Virtual Hosts:**
   ```
   Virtual Host:
   - Authority: housler.ru, www.housler.ru
   - Routes:
     ✓ Path: /
     ✓ Backend Group: <ваша группа>
     ✓ Timeout: 300s
   ```

4. **Если маршрута нет - СОЗДАЙТЕ:**
   ```
   Virtual Hosts → Add Route
   - Path: /
   - Backend Group: выберите вашу группу
   - Timeout: 300
   - Save
   ```

---

## 🚀 АЛЬТЕРНАТИВА: Обойти балансер (временно)

Если нужно **СРОЧНО** восстановить работу сайта:

### Вариант A: Изменить DNS на прямой IP

1. **Зайдите в вашу DNS панель** (где настроен housler.ru)

2. **Измените A-запись:**
   ```
   Было: housler.ru → <IP балансера>
   Стало: housler.ru → 91.229.8.221
   ```

3. **Подождите 5-10 минут** (TTL DNS)

4. **Проверьте:**
   ```bash
   curl https://housler.ru/health
   ```

**⚠️ Минусы:**
- Нет балансировки нагрузки
- Нет защиты от DDoS
- Один сервер - single point of failure

**✅ Плюсы:**
- Работает СРАЗУ после изменения DNS
- Простое решение
- Можно вернуть балансер потом

### Вариант B: Удалить балансер из цепочки

1. **Временно отключите балансер**
2. **Настройте nginx на сервере для HTTPS**
3. **Используйте Let's Encrypt для SSL**

---

## 📝 КАК ПРАВИЛЬНО ДЕПЛОИТЬ В ПРОД

### Процесс деплоя (когда прод работает):

#### Шаг 1: Подготовка локально

```bash
# Убедитесь что вы на нужной ветке
git branch
# Должно быть: main (или ваша рабочая ветка)

# Проверьте что все закоммичено
git status
# Должно быть: nothing to commit, working tree clean

# Если есть изменения - закоммитьте
git add .
git commit -m "feat: описание изменений"
```

#### Шаг 2: Деплой на прод

**Способ A: Автоматический (рекомендуется)**

```bash
# Запустить скрипт деплоя
bash scripts/deploy.sh

# Скрипт сделает:
# 1. Проверит незакоммиченные изменения
# 2. Запушит в GitHub
# 3. Подключится к серверу по SSH
# 4. Сделает git pull на сервере
# 5. Обновит зависимости (если нужно)
# 6. Перезапустит сервис
# 7. Проверит статус
# 8. Покажет логи
```

**Способ B: Ручной (если скрипт не работает)**

```bash
# 1. Запушить код в GitHub
git push origin main

# 2. SSH на сервер
ssh root@91.229.8.221
# Или с ключом:
ssh -i ~/.ssh/id_housler root@91.229.8.221

# 3. На сервере: перейти в директорию
cd /var/www/housler

# 4. Подтянуть изменения
git pull origin main

# 5. Обновить зависимости (если requirements.txt изменился)
source venv/bin/activate
pip install -r requirements.txt

# 6. Перезапустить приложение
systemctl restart housler

# 7. Проверить статус
systemctl status housler

# 8. Проверить логи
journalctl -u housler -n 50

# 9. Проверить что работает
curl http://localhost:5000/health
# Должно вернуть: {"status": "healthy", ...}

# 10. Выйти с сервера
exit
```

#### Шаг 3: Проверка после деплоя

```bash
# 1. Проверить главную страницу
curl https://housler.ru/

# 2. Проверить health endpoint
curl https://housler.ru/health

# 3. Проверить новый функционал (блог)
curl https://housler.ru/blog

# 4. Если что-то не работает - запустить диагностику
bash scripts/diagnose.sh

# 5. Посмотреть логи на сервере
ssh root@91.229.8.221 'journalctl -u housler -n 50 --no-pager'
```

#### Шаг 4: Откат (если что-то пошло не так)

```bash
# Откат на предыдущую версию
ssh root@91.229.8.221 'cd /var/www/housler && git reset --hard HEAD~1 && systemctl restart housler'

# Или откат на конкретный коммит
ssh root@91.229.8.221 'cd /var/www/housler && git checkout <commit_hash> && systemctl restart housler'

# Проверить что откат помог
curl https://housler.ru/health
```

---

## 🎯 БЫСТРЫЕ КОМАНДЫ

### Проверка статуса

```bash
# Диагностика (локально)
bash scripts/diagnose.sh

# Статус сервиса (на сервере)
ssh root@91.229.8.221 'systemctl status housler'

# Логи в реальном времени (на сервере)
ssh root@91.229.8.221 'journalctl -u housler -f'

# Проверка что приложение работает локально на сервере
ssh root@91.229.8.221 'curl -s http://localhost:5000/health | jq'
```

### Деплой

```bash
# Быстрый деплой
bash scripts/deploy.sh

# Или вручную
git push origin main
ssh root@91.229.8.221 'cd /var/www/housler && git pull && systemctl restart housler'
```

### Перезапуск

```bash
# Перезапустить только приложение
ssh root@91.229.8.221 'systemctl restart housler'

# Перезапустить nginx
ssh root@91.229.8.221 'systemctl restart nginx'

# Проверить статус после перезапуска
ssh root@91.229.8.221 'systemctl status housler'
```

### Логи

```bash
# Последние 50 строк
ssh root@91.229.8.221 'journalctl -u housler -n 50 --no-pager'

# В реальном времени
ssh root@91.229.8.221 'journalctl -u housler -f'

# Файловые логи
ssh root@91.229.8.221 'tail -100 /var/log/housler/error.log'
```

---

## ⚡ ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС

### Срочный план действий (по приоритету):

#### ✅ Вариант 1: Исправить балансер (правильно, но дольше)

1. Откройте Yandex Cloud Console
2. Application Load Balancer → Security Groups
3. Добавьте правила для портов 80 и 443
4. Проверьте Backend Group Health Check
5. Подождите 1-2 минуты
6. Проверьте: `curl https://housler.ru/health`

**Время:** 5-10 минут
**Риск:** Низкий
**Результат:** Полностью работающая инфраструктура

---

#### ✅ Вариант 2: Обойти балансер (быстро, но временно)

1. Зайдите в DNS панель (где настроен housler.ru)
2. Измените A-запись: housler.ru → 91.229.8.221
3. Подождите 5-10 минут (DNS propagation)
4. Проверьте: `curl https://housler.ru/health`

**Время:** 10-15 минут
**Риск:** Средний (нет балансировки)
**Результат:** Сайт работает напрямую

---

#### ✅ Вариант 3: Проверить сервер напрямую (диагностика)

```bash
# 1. Подключиться к серверу
ssh root@91.229.8.221

# 2. Проверить что приложение работает
curl http://localhost:5000/health

# 3. Если работает → проблема точно в балансере
# 4. Если НЕ работает → проблема на сервере

# 5. Проверить статус
systemctl status housler

# 6. Посмотреть логи
journalctl -u housler -n 50

# 7. Перезапустить если нужно
systemctl restart housler
```

**Время:** 2-3 минуты
**Риск:** Нет
**Результат:** Понимание где именно проблема

---

## 📞 Чек-лист диагностики

Если сайт не работает - используйте этот чек-лист:

```
□ Запустить bash scripts/diagnose.sh
□ Проверить HTTP код: curl -I https://housler.ru/

Если 403:
  □ Открыть Yandex Cloud Console
  □ ALB → Security Groups → проверить правила
  □ Backend Groups → проверить Health Check
  □ HTTP Router → проверить маршруты

Если 502:
  □ SSH на сервер: ssh root@91.229.8.221
  □ Проверить статус: systemctl status housler
  □ Перезапустить: systemctl restart housler
  □ Проверить логи: journalctl -u housler -f

Если 500:
  □ SSH на сервер
  □ Логи приложения: tail -f /var/log/housler/error.log
  □ Откатиться: git reset --hard HEAD~1
  □ Перезапустить: systemctl restart housler

Если сайт вообще не открывается:
  □ Проверить DNS: nslookup housler.ru
  □ Проверить сервер: ping 91.229.8.221
  □ Проверить SSH: ssh root@91.229.8.221
```

---

## 🎓 ИТОГО

### Проблема:
**HTTP 403 от Yandex Cloud ALB (Envoy proxy)**

### Причина:
**Security Group блокирует входящий трафик на балансер**

### Решение:
1. **Быстрое:** Изменить DNS на прямой IP (91.229.8.221)
2. **Правильное:** Исправить Security Groups в Yandex Cloud

### Как деплоить:
```bash
git commit && git push
bash scripts/deploy.sh
curl https://housler.ru/health  # проверка
```

---

**Нужна помощь?** Запустите `bash scripts/diagnose.sh` и пришлите результат.

**Срочно нужно починить?** Измените DNS на 91.229.8.221
