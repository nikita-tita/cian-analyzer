# 🔍 Проверка типа Railway токена

## Проблема
Токен не работает с Railway API. Вероятная причина: неправильный тип токена.

## Типы токенов Railway

### ❌ Project Token (НЕ ПОДХОДИТ для API)
- Создаётся: Project Settings → Tokens
- Область: только один проект/окружение
- **НЕ РАБОТАЕТ с Public API**
- Используется только для CI/CD этого проекта

### ✅ Account Token (НУЖЕН для API)
- Создаётся: **Account Settings** → Tokens
- Область: все ваши ресурсы и команды
- **РАБОТАЕТ с Public API**
- Может создавать новые проекты

### ✅ Team Token (альтернатива)
- Создаётся: Team Settings → Tokens
- Область: все ресурсы команды
- **РАБОТАЕТ с Public API**
- Может создавать проекты в команде

## 🎯 Решение

### Проверьте, где вы создавали токен:

**Если вы создавали в:**
```
Account Settings (вверху справа) → Tokens → Create Token
```
✅ Это правильно - Account Token

**Если вы создавали в:**
```
Project → Settings → Tokens → Create Token
```
❌ Это неправильно - Project Token (не работает с API)

## 🔄 Если у вас Project Token:

### Шаг 1: Удалите старый токен
1. Project Settings → Tokens
2. Удалите токен

### Шаг 2: Создайте Account Token
1. Кликните на свой аватар (правый верхний угол)
2. **Account Settings** (не Project Settings!)
3. В боковом меню: **Tokens**
4. **Create Token**
5. Название: "API Deploy Token"
6. Сохраните токен - покажется только один раз!

### Шаг 3: Дайте мне новый токен
```bash
# Я запущу:
bash deploy-railway.sh НОВЫЙ_ТОКЕН
```

---

## 🧪 Тест текущего токена

Ваш текущий токен: `f335b5d8-79f3-4c1c-8cf1-fc041d9c6014`

Результат теста: **403 Access denied**

Это значит либо:
1. ❌ Это Project Token (не подходит)
2. ❌ Токен был деактивирован
3. ❌ Недостаточно прав

---

## 💡 Альтернатива: GitHub Integration

Если не хотите возиться с токенами:

**Вариант 1: Railway UI (2 минуты)**
```
1. railway.app/new
2. Deploy from GitHub
3. Выберите: nikita-tita/cian-analyzer
4. Ветка: claude/review-project-011CUrTS5jNGPrP8p61s7prx
5. + New → Add Redis
6. + New → Add PostgreSQL
7. Generate Domain
```

**Вариант 2: Railway CLI + GitHub**
```bash
# Если CLI установлен:
railway link
railway up
```

Скажите, какой вариант предпочитаете? 🚀
