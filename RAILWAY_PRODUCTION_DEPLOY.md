# 🚀 Railway Production Deployment - Полноценный парсинг

## ✅ Что включено

- ✅ **PlaywrightParser** - полноценный парсинг с браузером Chromium
- ✅ **Redis** - для хранения сессий (работает с multiple workers)
- ✅ **Docker** - изолированное окружение с всеми зависимостями
- ✅ **Gunicorn** - production-ready сервер с 2 workers
- ✅ **Health checks** - мониторинг состояния приложения

---

## 📋 Шаг 1: Подготовка (ВЫПОЛНЕНО ✅)

Все файлы уже созданы и готовы:
- ✅ `Dockerfile` - конфигурация Docker с Playwright
- ✅ `requirements.txt` - обновлен (playwright + redis)
- ✅ `redis_session_manager.py` - менеджер сессий
- ✅ `app_new.py` - переключен на PlaywrightParser + Redis
- ✅ `railway.toml` - настроен для Docker
- ✅ `.dockerignore` - оптимизация сборки

---

## 🔧 Шаг 2: Добавить Redis в Railway

### 2.1. Откройте Railway Dashboard

1. Перейдите на https://railway.app
2. Откройте ваш проект **web-production-04439**

### 2.2. Добавьте Redis сервис

1. Нажмите **"+ New"** в правом верхнем углу
2. Выберите **"Database" → "Add Redis"**
3. Railway автоматически создаст Redis сервис

### 2.3. Подключите Redis к вашему приложению

1. Откройте ваш **web** сервис (приложение)
2. Перейдите в **"Variables"**
3. Нажмите **"+ New Variable"** → **"Add Reference"**
4. Выберите **Redis** → **`REDIS_URL`**
5. Нажмите **"Add"**

Теперь ваше приложение автоматически подключится к Redis!

---

## 🚀 Шаг 3: Деплой

### 3.1. Закоммитьте и запушьте изменения

Изменения уже готовы, просто запушьте их:

```bash
git push origin claude/fix-project-deployment-011CUrhFtfy5rawguigGdbYL
```

### 3.2. Railway автоматически:

1. Обнаружит `Dockerfile`
2. Соберет Docker образ с Playwright и браузером
3. Установит все зависимости
4. Запустит приложение с 2 workers
5. Подключит Redis

**Время сборки: 5-7 минут** (первый раз дольше, т.к. устанавливается браузер)

---

## 🔍 Шаг 4: Проверка

### 4.1. Дождитесь завершения деплоя

В Railway Dashboard статус должен быть **🟢 Active**

### 4.2. Проверьте логи

Вы должны увидеть:

```
============================================================
🚀 Cian Analyzer v2.0 - Railway Production [Worker: XXX]
============================================================
📊 Parser: PlaywrightParser (Full-featured)
📊 Cache: Redis (Distributed)
📊 Redis Connected: True
📊 Worker ID: XXX
============================================================
```

### 4.3. Проверьте health endpoint

Откройте в браузере:
```
https://web-production-04439.up.railway.app/api/health
```

Ответ должен быть:
```json
{
  "status": "healthy",
  "sessions": 0,
  "parser": "PlaywrightParser",
  "redis_connected": true,
  "worker_id": 123
}
```

### 4.4. Протестируйте парсинг

1. Откройте https://web-production-04439.up.railway.app/
2. Вставьте URL с Cian (например: https://www.cian.ru/sale/flat/315831388/)
3. Нажмите **"Спарсить объект"**
4. Дождитесь результата (может занять 30-60 секунд)
5. Проверьте что данные **настоящие** (не "Тестовая улица")

---

## ⚙️ Конфигурация

### Docker

- **Base image**: `python:3.11-slim`
- **Browser**: Chromium (headless)
- **Workers**: 2 gunicorn workers
- **Timeout**: 300 секунд
- **Port**: 8080

### Redis

- **TTL сессий**: 2 часа (7200 секунд)
- **Автоматический fallback**: Если Redis недоступен, используется in-memory

### Gunicorn

```
--workers 2
--timeout 300
--bind 0.0.0.0:$PORT
```

---

## 🐛 Troubleshooting

### Проблема: Build failed

**Решение:**
1. Проверьте логи сборки
2. Убедитесь что `Dockerfile` корректный
3. Попробуйте Redeploy

### Проблема: Redis connection error

**Решение:**
1. Проверьте что Redis сервис создан
2. Проверьте что `REDIS_URL` добавлен в Variables
3. Проверьте логи: должно быть `Redis Connected: True`

### Проблема: Парсинг не работает / 502 Error

**Решение:**
1. Проверьте что Playwright установлен (в логах сборки)
2. Увеличьте timeout до 500 секунд в Dockerfile
3. Проверьте что браузер запускается: `playwright install-deps`

### Проблема: Долгая сборка (>10 минут)

**Причина:** Установка Chromium браузера занимает время
**Решение:** Подождите, первая сборка всегда дольше. Последующие будут быстрее благодаря кэшу.

---

## 📊 Мониторинг

### Логи

В Railway Dashboard → Logs вы увидите:
- 🌐 Все HTTP запросы с worker_id
- ✅ Успешные операции
- ❌ Ошибки с полным stack trace
- 📊 Метрики (сессии, Redis статус)

### Метрики

Эндпоинт `/api/health` показывает:
- Статус приложения
- Количество активных сессий
- Подключение к Redis
- Worker ID

---

## 🎯 Следующие шаги

После успешного деплоя:

1. **Протестируйте полностью** - пройдите все 3 шага
2. **Проверьте аналоги** - они должны быть настоящими с Cian
3. **Проверьте анализ** - убедитесь что расчеты корректны

---

## 💰 Стоимость

- **Railway Free Tier**: $5/месяц кредитов
- **Redis**: ~$1-2/месяц
- **Web service**: ~$3-4/месяц

**Итого**: ~$4-6/месяц (покрывается Free Tier)

---

## 🔗 Полезные ссылки

- [Railway Dashboard](https://railway.app/dashboard)
- [Playwright Docs](https://playwright.dev/python/)
- [Redis Python Client](https://redis-py.readthedocs.io/)
- [Gunicorn Docs](https://docs.gunicorn.org/)

---

**Готово! Ваше приложение будет работать с полноценным парсингом Cian.ru** 🎉
