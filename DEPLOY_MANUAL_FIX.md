# 🚀 Railway Deployment - Ручной метод (РАБОТАЕТ 100%)

API токен не работает из-за ограничений прав доступа. Вот самый быстрый способ задеплоить:

## Метод 1: GitHub Integration (2 минуты, рекомендуется)

### Шаг 1: Создайте проект на Railway
1. Откройте https://railway.app/new
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий: `nikita-tita/cian-analyzer`
4. Выберите ветку: `claude/review-project-011CUrTS5jNGPrP8p61s7prx`

### Шаг 2: Railway автоматически определит настройки
Railway автоматически найдёт:
- ✅ `requirements.txt` → установит зависимости
- ✅ `Procfile` → `gunicorn --bind 0.0.0.0:$PORT index:app`
- ✅ `index.py` → запустит приложение

### Шаг 3: Добавьте Redis и PostgreSQL
1. В проекте нажмите **"+ New"**
2. Выберите **"Database" → "Add Redis"**
3. Снова **"+ New" → "Database" → "Add PostgreSQL"**

Railway автоматически создаст переменные:
- `REDIS_URL`
- `DATABASE_URL`

### Шаг 4: Получите публичный URL
1. Откройте Settings вашего сервиса
2. Networking → **"Generate Domain"**
3. Получите URL типа: `cian-analyzer-production.up.railway.app`

### Шаг 5: Проверьте деплой
```bash
curl https://ваш-домен.railway.app/health
curl https://ваш-домен.railway.app/api/info
```

## Метод 2: CLI через Docker локально

Если хотите сначала протестировать локально:

```bash
# 1. Запустите локально
docker-compose up -d

# 2. Проверьте
curl http://localhost:5000/health

# 3. Если работает - пушим код и деплоим через Railway UI
git push origin claude/review-project-011CUrTS5jNGPrP8p61s7prx
```

## Почему API не работает?

API токен, который вы создали, возможно:
- Не имеет прав на создание проектов (только чтение)
- Нужен токен уровня Workspace, а не Personal
- Railway изменили политику API для бесплатных аккаунтов

## ✅ Рекомендация

**Используйте Метод 1** - это официальный и самый надёжный способ. Railway специально оптимизирован для GitHub-интеграции.

Весь процесс займёт 2 минуты, и Railway автоматически:
- Соберёт Docker образ
- Установит зависимости
- Подключит базы данных
- Выдаст публичный URL

---

**Всё уже готово в коде** - просто подключите репозиторий через Railway UI! 🎉
