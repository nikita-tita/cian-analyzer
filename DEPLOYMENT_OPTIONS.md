# 🚀 Варианты деплоя Cian Analyzer

Этот документ описывает **все доступные варианты деплоя** с автоматической настройкой. Вам нужно только подключить GitHub репозиторий!

## 📋 Обзор вариантов

| Платформа | Сложность | Бесплатный план | Парсинг | Время деплоя |
|-----------|-----------|----------------|---------|--------------|
| **Vercel** | ⭐ Легко | ✅ Да (Hobby) | ⚠️ Demo-режим | ~2 мин |
| **Railway** | ⭐ Легко | ✅ $5/месяц кредитов | ✅ Полный | ~3 мин |
| **Render** | ⭐ Легко | ✅ Да (Free tier) | ✅ Полный | ~5 мин |
| **Fly.io** | ⭐⭐ Средне | ✅ Да (256MB RAM) | ✅ Полный | ~4 мин |
| **Google Cloud Run** | ⭐⭐⭐ Сложнее | ✅ Да (щедрый) | ✅ Полный | ~6 мин |

---

## 1️⃣ Vercel (Рекомендуется для быстрого старта)

### ✅ Преимущества
- Самый быстрый деплой (1 клик)
- Автоматические HTTPS
- CDN по всему миру
- Отличная интеграция с GitHub

### ⚠️ Ограничения
- Парсинг работает в demo-режиме (нет Playwright)
- Serverless timeout 10-60 сек
- Подходит для ручного ввода данных

### 🚀 Деплой за 3 шага

1. **Перейдите на Vercel**
   ```
   https://vercel.com/new
   ```

2. **Import Git Repository**
   - Нажмите "Add New" → "Project"
   - Выберите ваш GitHub репозиторий `cian-analyzer`
   - Нажмите "Import"

3. **Deploy**
   - Все настройки уже готовы в `vercel.json`
   - Нажмите "Deploy"
   - Готово! 🎉

### 📝 Что уже настроено
- ✅ `vercel.json` - конфигурация
- ✅ `index.py` - entry point
- ✅ `requirements.txt` - зависимости

---

## 2️⃣ Railway (Рекомендуется для полного функционала)

### ✅ Преимущества
- **Полный парсинг с Playwright**
- Простой UI
- Automatic deployments
- $5 бесплатных кредитов каждый месяц

### 🚀 Деплой за 4 шага

1. **Перейдите на Railway**
   ```
   https://railway.app/
   ```

2. **Войдите через GitHub**
   - Нажмите "Login with GitHub"

3. **New Project**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите `cian-analyzer`

4. **Deploy**
   - Railway автоматически найдет `railway.json` и `Procfile`
   - Деплой начнется автоматически
   - Получите свой URL! 🎉

### 📝 Что уже настроено
- ✅ `railway.json` - конфигурация
- ✅ `railway.toml` - build настройки
- ✅ `Procfile` - команда запуска

### 📖 Подробная инструкция
См. [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md)

---

## 3️⃣ Render (Альтернатива Railway)

### ✅ Преимущества
- **Полный парсинг с Playwright**
- Бесплатный tier навсегда
- Простая настройка
- Automatic SSL

### ⚠️ Ограничения
- Free tier засыпает после 15 мин неактивности
- Холодный старт ~30 секунд

### 🚀 Деплой за 5 шагов

1. **Перейдите на Render**
   ```
   https://render.com/
   ```

2. **Войдите через GitHub**
   - Нажмите "Get Started" → "Sign in with GitHub"

3. **New Web Service**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

4. **Configure**
   - Render автоматически найдет `render.yaml`
   - Или настройте вручную:
     - **Name**: cian-analyzer
     - **Environment**: Python 3
     - **Build Command**: `pip install -r requirements.txt`
     - **Start Command**: `gunicorn --bind 0.0.0.0:$PORT index:app`

5. **Deploy**
   - Нажмите "Create Web Service"
   - Готово! 🎉

### 📝 Что уже настроено
- ✅ `render.yaml` - полная конфигурация
- ✅ Gunicorn в requirements.txt

### 💡 Совет
Для production используйте платный план ($7/мес) - без засыпания и быстрее.

---

## 4️⃣ Fly.io (Для продвинутых)

### ✅ Преимущества
- **Полный парсинг с Playwright**
- Запуск близко к пользователям (edge locations)
- Бесплатный tier: 3 VM × 256MB RAM
- Поддержка Docker
- Автоматическое масштабирование

### 🚀 Деплой за 6 шагов

1. **Установите Fly CLI**
   ```bash
   # macOS
   brew install flyctl

   # Linux
   curl -L https://fly.io/install.sh | sh

   # Windows
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **Войдите в аккаунт**
   ```bash
   flyctl auth login
   ```

3. **Перейдите в папку проекта**
   ```bash
   cd cian-analyzer
   ```

4. **Запустите деплой**
   ```bash
   flyctl launch
   ```

   Fly CLI спросит:
   - "Would you like to copy its configuration?" → **Yes**
   - "Would you like to deploy now?" → **Yes**

5. **Готово!**
   ```bash
   flyctl open
   ```

### 📝 Что уже настроено
- ✅ `fly.toml` - конфигурация
- ✅ `Dockerfile` - образ приложения

### 🔧 Полезные команды
```bash
# Посмотреть логи
flyctl logs

# Открыть приложение
flyctl open

# Статус
flyctl status

# Масштабирование
flyctl scale count 2

# Редеплой
flyctl deploy
```

---

## 5️⃣ Google Cloud Run (Для корпоративных проектов)

### ✅ Преимущества
- **Полный парсинг с Playwright**
- Самый щедрый бесплатный tier (2 млн запросов/месяц)
- Автоматическое масштабирование
- Интеграция с Google Cloud
- Pay-per-use (платишь только за использование)

### ⚠️ Требования
- Google Cloud аккаунт (требует кредитную карту для верификации)
- Создать проект в Google Cloud

### 🚀 Деплой за 7 шагов

1. **Установите Google Cloud SDK**
   ```bash
   # macOS
   brew install --cask google-cloud-sdk

   # Linux
   curl https://sdk.cloud.google.com | bash

   # Windows - скачайте установщик:
   # https://cloud.google.com/sdk/docs/install
   ```

2. **Войдите в аккаунт**
   ```bash
   gcloud auth login
   ```

3. **Создайте проект (если нет)**
   ```bash
   gcloud projects create cian-analyzer-PROJECT-ID --name="Cian Analyzer"
   gcloud config set project cian-analyzer-PROJECT-ID
   ```

4. **Включите необходимые API**
   ```bash
   gcloud services enable cloudbuild.googleapis.com run.googleapis.com
   ```

5. **Перейдите в папку проекта**
   ```bash
   cd cian-analyzer
   ```

6. **Запустите деплой**
   ```bash
   gcloud run deploy cian-analyzer \
     --source . \
     --region=europe-west1 \
     --allow-unauthenticated \
     --memory=512Mi \
     --cpu=1 \
     --timeout=30s \
     --max-instances=3 \
     --min-instances=0
   ```

7. **Готово!**
   Cloud Run выдаст URL вашего приложения.

### 📝 Что уже настроено
- ✅ `Dockerfile` - образ приложения
- ✅ `cloudbuild.yaml` - CI/CD конфигурация
- ✅ `.gcloudignore` - исключения для деплоя

### 🤖 Автоматический деплой через GitHub

Настройте Cloud Build trigger:

1. Перейдите в Cloud Console → Cloud Build → Triggers
2. Нажмите "Create Trigger"
3. Выберите ваш GitHub репозиторий
4. Настройте:
   - **Event**: Push to branch
   - **Branch**: `^main$`
   - **Configuration**: Cloud Build configuration file
   - **Location**: `/cloudbuild.yaml`
5. Сохраните

Теперь каждый push в main будет автоматически деплоить!

### 🔧 Полезные команды
```bash
# Посмотреть логи
gcloud run services logs read cian-analyzer --region=europe-west1

# Список сервисов
gcloud run services list

# Удалить сервис
gcloud run services delete cian-analyzer --region=europe-west1
```

---

## 📊 Сравнение стоимости

### Бесплатные лимиты

| Платформа | RAM | CPU | Запросов/мес | Время работы |
|-----------|-----|-----|--------------|--------------|
| **Vercel** | 1GB | 1 | Безлимит | 100 GB-часов |
| **Railway** | 512MB | 1 vCPU | ~$5 кредитов | ~500 часов |
| **Render** | 512MB | 0.5 | Безлимит | Безлимит (с засыпанием) |
| **Fly.io** | 256MB×3 | 1 shared | Безлимит | Безлимит |
| **Cloud Run** | 1GB | 1 | 2 млн | 180k vCPU-сек/мес |

### Платные планы (примерно)

| Платформа | Цена/месяц | За что |
|-----------|------------|--------|
| **Vercel** | $20 | Pro (команды, больше лимитов) |
| **Railway** | ~$5-10 | Pay-per-use |
| **Render** | $7 | Starter (без засыпания) |
| **Fly.io** | ~$5-10 | Pay-per-use |
| **Cloud Run** | ~$5-15 | Pay-per-use (очень гибко) |

---

## 🎯 Какую платформу выбрать?

### Для быстрого теста
→ **Vercel** - деплой за 1 минуту, но demo-парсинг

### Для полного функционала (простота)
→ **Railway** или **Render** - просто и работает из коробки

### Для продакшена с трафиком
→ **Google Cloud Run** - масштабируется автоматически, платите только за использование

### Для экспериментов с Docker
→ **Fly.io** - современная платформа, edge computing

---

## 🔧 Общие настройки

### Environment Variables (если нужны)

Для всех платформ можно добавить переменные окружения:

```bash
FLASK_ENV=production
SECRET_KEY=your-secret-key-here
USER_AGENT=Mozilla/5.0...
```

**Где добавлять:**
- **Vercel**: Settings → Environment Variables
- **Railway**: Variables tab
- **Render**: Environment tab
- **Fly.io**: `flyctl secrets set KEY=value`
- **Cloud Run**: `--set-env-vars KEY=value`

---

## 🐛 Troubleshooting

### Приложение не запускается

1. Проверьте логи:
   - Vercel: Deployments → View Function Logs
   - Railway: Deployments → Logs
   - Render: Logs tab
   - Fly.io: `flyctl logs`
   - Cloud Run: Cloud Console → Cloud Run → Logs

2. Проверьте, что все файлы на месте:
   ```bash
   git status
   git push
   ```

### Ошибка 502/503

- Проверьте, что приложение слушает правильный порт (`$PORT`)
- Увеличьте timeout (для Render/Cloud Run)

### Парсинг не работает

- На Vercel это нормально (demo-режим)
- Для полного парсинга используйте Railway/Render/Fly.io/Cloud Run

---

## 📚 Дополнительные ресурсы

- [Официальная документация Vercel](https://vercel.com/docs)
- [Официальная документация Railway](https://docs.railway.app)
- [Официальная документация Render](https://render.com/docs)
- [Официальная документация Fly.io](https://fly.io/docs)
- [Официальная документация Cloud Run](https://cloud.google.com/run/docs)

---

## 🎉 Готово!

Все конфигурационные файлы уже созданы:
- ✅ `vercel.json` - Vercel
- ✅ `railway.json`, `railway.toml`, `Procfile` - Railway
- ✅ `render.yaml` - Render
- ✅ `fly.toml`, `Dockerfile` - Fly.io
- ✅ `cloudbuild.yaml`, `.gcloudignore` - Google Cloud Run

**Вам нужно только выбрать платформу и подключить GitHub репозиторий!** 🚀

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
