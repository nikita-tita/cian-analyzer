# PR: Восстановить отображение рекомендаций в калькуляторе

## 🎯 Описание проблемы

Потеряны UI доработки:
- ❌ В отчете не отображаются рекомендации
- ✅ Кнопки в хэдере (уже исправлены ранее)
- ✅ Логотип Housler кликабельный (уже исправлен ранее)

## ✅ Что исправлено

### 1. Рекомендации в UI
**Проблема:** Контекстный анализ был отключен + контейнер скрыт
**Исправлено:**
- `src/analytics/recommendations.py:107` - включил `_analyze_adjustments_context()`
- `static/js/wizard.js:1236` - добавил показ контейнера рекомендаций

### 2. Кнопки в хэдере ✅
**Статус:** УЖЕ ИСПРАВЛЕНО в предыдущих коммитах
- Кнопка "Поделиться" правильная (`btn-outline-dark`)
- Симметрично расположена

### 3. Логотип Housler ✅
**Статус:** УЖЕ ИСПРАВЛЕНО в предыдущих коммитах
- `templates/wizard.html:26` - логотип кликабельный → `/#services`

---

## 📋 Изменения

### Файлы
```
src/analytics/recommendations.py | -3 строки (удалил комментарии)
                                 | +1 строка (включил контекстный анализ)
static/js/wizard.js              | +3 строки (показ контейнера)
```

### Детали изменений

#### `src/analytics/recommendations.py`
```python
# БЫЛО (строки 106-109):
# 5. Контекстный анализ корректировок
# ВРЕМЕННО ОТКЛЮЧЕНО: вызывает проблемы производительности
# TODO: Оптимизировать _analyze_adjustments_context()
# recommendations.extend(self._analyze_adjustments_context())

# СТАЛО (строки 106-107):
# 5. Контекстный анализ корректировок
recommendations.extend(self._analyze_adjustments_context())
```

#### `static/js/wizard.js`
```javascript
// БЫЛО (строки 1229-1233):
if (html === '') {
    html = '<p class="text-muted">Нет рекомендаций для данного объекта</p>';
}
container.innerHTML = html;

// СТАЛО (строки 1229-1236):
if (html === '') {
    html = '<p class="text-muted">Нет рекомендаций для данного объекта</p>';
}
container.innerHTML = html;

// Показываем контейнер рекомендаций
document.getElementById('recommendations-container').style.display = 'block';
```

---

## ✅ Проверки

- ✅ Python синтаксис: `recommendations.py`, `analyzer.py`, `markdown_exporter.py`
- ✅ JavaScript синтаксис: `wizard.js` (проверено через Node.js)
- ✅ Коэффициенты отделки правильные (1.12, 1.15, 1.18)
- ✅ Git: коммит создан, запушен в `claude/fix-issues-011CV1ygKxZKE7JVbYFNxZDn`

---

## 🧪 Тестирование

**Как проверить:**
1. Запустить калькулятор
2. Загрузить объект (через URL или вручную)
3. Найти аналоги
4. Перейти к анализу
5. **Проверить:** блок "💡 Персональные рекомендации" должен быть виден
6. **Проверить:** рекомендации отображаются с приоритетами (КРИТИЧНО, ВАЖНО, СРЕДНЕ, ИНФО)

---

## 🚀 Деплой через Git

### Шаг 1: Создать PR на GitHub
```bash
# Откройте в браузере:
https://github.com/nikita-tita/cian-analyzer/pull/new/claude/fix-issues-011CV1ygKxZKE7JVbYFNxZDn

# Или используйте gh CLI (если доступен):
gh pr create --title "fix: Восстановить отображение рекомендаций" \
             --body "См. PR_FIX_RECOMMENDATIONS.md" \
             --base main
```

### Шаг 2: Мердж в main
После ревью и аппрува - мердж в main через GitHub UI

### Шаг 3: Деплой на production сервер

**Вариант А: Автодеплой (если настроен)**
```bash
# После мерджа в main автоматически запустится GitHub Action
# Проверить статус: https://github.com/nikita-tita/cian-analyzer/actions
```

**Вариант Б: Ручной деплой**
```bash
# Подключиться к серверу
ssh user@housler.ru

# Перейти в директорию проекта
cd /path/to/cian-analyzer

# Получить изменения
git fetch origin
git checkout main
git pull origin main

# Быстрый рестарт сервисов
./scripts/quick-restart.sh

# Или полный деплой (если нужны новые зависимости)
./scripts/deploy-production.sh
```

### Шаг 4: Проверка после деплоя
```bash
# Проверить статус
./scripts/check-status.sh

# Проверить логи
./scripts/logs.sh

# Открыть сайт
https://housler.ru/calculator
```

---

## 📊 Статистика PR

**Коммиты:** 1
- `a4d0f13` - fix: Восстановить отображение рекомендаций в калькуляторе

**Файлы:** 2
- `src/analytics/recommendations.py`
- `static/js/wizard.js`

**Строки:**
- 4 строки добавлено
- 3 строки удалено

---

## 🔗 Ссылки

- **Branch:** `claude/fix-issues-011CV1ygKxZKE7JVbYFNxZDn`
- **Base:** `main`
- **Коммит:** `a4d0f13`
- **GitHub PR:** https://github.com/nikita-tita/cian-analyzer/pull/new/claude/fix-issues-011CV1ygKxZKE7JVbYFNxZDn
