# 📥 Как скачать результаты парсинга

## 🚀 Быстрые способы

### 1. Автоматическое скачивание в Downloads (рекомендуется!)

```bash
python3 download_latest.py
```

**Что происходит:**
- Находит последний созданный Markdown файл
- Копирует его в папку `~/Downloads` с уникальным именем
- Показывает полный путь к файлу

**Результат:**
```
✅ УСПЕШНО СКАЧАНО!
📁 Сохранено в: ~/Downloads/cian_export_20251104_194049.md
```

---

### 2. Универсальный скрипт (всё в одном)

```bash
python3 download_results.py
```

**Интерактивное меню:**
```
1. 📁 Скопировать последний результат в Downloads
2. 📋 Скопировать в буфер обмена
3. 📂 Экспортировать в выбранное место
4. 📊 Показать все доступные файлы
5. ❌ Выход
```

Выберите нужное действие и следуйте инструкциям!

---

### 3. Копирование в буфер обмена

```bash
python3 copy_to_clipboard.py
```

**Что происходит:**
- Читает последний Markdown файл
- Копирует его содержимое в буфер обмена
- Теперь можете вставить (Cmd/Ctrl+V) куда угодно

**Используется для:**
- Вставки в Google Docs
- Отправки в Telegram/Slack
- Вставки в Notion
- И т.д.

---

### 4. Экспорт с выбором места

```bash
python3 export_to_file.py
```

**Интерактивный выбор:**
1. Выбираете куда сохранить (Downloads, Desktop, своя папка)
2. Указываете имя файла (или оставляете автоматическое)
3. Файл экспортируется в выбранное место

---

## 📁 Где находятся файлы?

### Исходные файлы (в папке проекта)

```bash
# Все созданные Markdown файлы
ls -lh cian_results_*.md demo_export.md

# Последний по времени
ls -lt *.md | head -1
```

### Скачанные файлы

**macOS:**
```bash
# Папка Downloads
~/Downloads/cian_export_*.md

# Открыть папку
open ~/Downloads
```

**Linux:**
```bash
~/Downloads/cian_export_*.md
```

**Windows:**
```bash
C:\Users\ВашеИмя\Downloads\cian_export_*.md
```

---

## 🎯 Примеры использования

### Пример 1: Быстрое скачивание

```bash
# Создаем результаты
python3 demo_markdown_export.py

# Скачиваем
python3 download_latest.py

# Файл в Downloads!
```

### Пример 2: Копирование в буфер

```bash
# Создаем результаты
python3 demo_markdown_export.py

# Копируем в буфер
python3 copy_to_clipboard.py

# Вставляем в любой редактор (Cmd/Ctrl+V)
```

### Пример 3: Экспорт на Desktop

```bash
# Запускаем универсальный скрипт
python3 download_results.py

# Выбираем:
# 3. Экспортировать в выбранное место
# 2. ~/Desktop
# Вводим имя: client_report.md

# Файл на рабочем столе!
```

---

## 💡 Ручные способы

### Простое копирование

```bash
# Копировать последний файл в Downloads
cp demo_export.md ~/Downloads/

# Копировать с новым именем
cp demo_export.md ~/Downloads/my_report.md

# Копировать все результаты
cp cian_results_*.md ~/Downloads/
```

### Через Finder (macOS) / Explorer (Windows)

1. Откройте папку проекта
2. Найдите нужный `.md` файл
3. Перетащите в нужное место
4. Готово!

### Через терминал с автодополнением

```bash
# Начните вводить и нажмите Tab
cp demo<Tab>
cp cian_<Tab>

# macOS: откроет в Finder
open demo_export.md

# Linux: откроет в файловом менеджере
xdg-open demo_export.md
```

---

## 🔄 Конвертация после скачивания

### В PDF

```bash
# Установите pandoc (один раз)
brew install pandoc  # macOS
# или apt-get install pandoc  # Linux

# Конвертируйте
pandoc ~/Downloads/cian_export_*.md -o report.pdf
```

### В HTML

```bash
pandoc ~/Downloads/cian_export_*.md -o report.html
```

### В DOCX (Word)

```bash
pandoc ~/Downloads/cian_export_*.md -o report.docx
```

---

## 📊 Работа с несколькими файлами

### Показать все файлы с результатами

```bash
python3 download_results.py
# Выберите: 4. Показать все доступные файлы
```

Или вручную:

```bash
ls -lht cian_results_*.md demo_export.md
```

### Скачать все файлы

```bash
# Копировать все в Downloads
cp cian_results_*.md demo_export.md ~/Downloads/

# Или создать архив
zip -r results.zip cian_results_*.md demo_export.md
mv results.zip ~/Downloads/
```

### Объединить несколько файлов

```bash
# Объединить все в один большой файл
cat cian_results_*.md > combined_results.md
```

---

## 🎨 Структура скачанных файлов

```
~/Downloads/
├── cian_export_20251104_194049.md    ← Скачан через download_latest.py
├── cian_export_20251104_183022.md    ← Предыдущий экспорт
└── my_custom_report.md               ← Экспорт с выбором имени
```

Каждый файл содержит:
- ✅ Полную информацию об объявлениях
- ✅ Ссылки на карты
- ✅ Изображения
- ✅ Красивое форматирование

---

## ⚡ Горячие команды

```bash
# Быстрое скачивание
python3 download_latest.py

# Интерактивное меню
python3 download_results.py

# В буфер обмена
python3 copy_to_clipboard.py

# Показать последний файл
ls -lt *.md | head -1

# Открыть последний файл
open $(ls -t *.md | head -1)

# Скопировать последний в Downloads
cp $(ls -t cian_results_*.md | head -1) ~/Downloads/
```

---

## 🤔 Часто задаваемые вопросы

### Где файл после скачивания?

**macOS/Linux:**
```bash
~/Downloads/cian_export_YYYYMMDD_HHMMSS.md
```

**Windows:**
```bash
C:\Users\ИМЯ\Downloads\cian_export_YYYYMMDD_HHMMSS.md
```

### Как открыть скачанный файл?

```bash
# macOS
open ~/Downloads/cian_export_*.md

# Linux
xdg-open ~/Downloads/cian_export_*.md

# Windows
start %USERPROFILE%\Downloads\cian_export_*.md
```

### Можно ли переименовать файл?

Да! При скачивании через `export_to_file.py` или `download_results.py` можно указать своё имя.

Или переименуйте после скачивания:

```bash
mv ~/Downloads/cian_export_*.md ~/Downloads/my_report.md
```

### Как отправить файл коллеге?

**Вариант 1: Через email**
- Прикрепите `.md` файл из Downloads

**Вариант 2: Через облако**
- Загрузите в Google Drive / Dropbox / iCloud

**Вариант 3: Конвертируйте в PDF**
```bash
pandoc ~/Downloads/cian_export_*.md -o report.pdf
```
- Отправьте PDF (более универсальный формат)

### Файл слишком большой?

Markdown файлы очень компактные! Но если нужно:

```bash
# Заархивируйте
zip report.zip ~/Downloads/cian_export_*.md

# Или используйте gzip
gzip -c ~/Downloads/cian_export_*.md > report.md.gz
```

---

## 🎯 Резюме

**Самый простой способ:**
```bash
python3 download_latest.py
```

**Универсальный способ:**
```bash
python3 download_results.py
```

**Для вставки в редактор:**
```bash
python3 copy_to_clipboard.py
```

**Ручное копирование:**
```bash
cp demo_export.md ~/Downloads/
```

---

## 📚 См. также

- [START_HERE.md](START_HERE.md) - С чего начать
- [QUICK_START_MARKDOWN.md](QUICK_START_MARKDOWN.md) - Быстрый старт
- [MARKDOWN_EXPORT.md](MARKDOWN_EXPORT.md) - Полная документация
- [README.md](README.md) - Основная документация

---

**Готово!** Теперь вы знаете все способы скачивания результатов! 🎉
