# 🚀 Быстрый старт: Экспорт в Markdown

## За 30 секунд

### 1. Посмотрите демо

```bash
python3 demo_markdown_export.py
open demo_export.md
```

Откроется файл с примерами форматирования.

### 2. Запустите парсинг с автоматическим экспортом

```bash
python3 parse_with_playwright.py
```

Автоматически создастся:
- `cian_results_YYYYMMDD_HHMMSS.md` ← **красиво отформатированный Markdown**
- `playwright_results_YYYYMMDD_HHMMSS.json` ← JSON для обработки

## Что вы получите в Markdown файле?

### 📋 Структура
```
# Результаты парсинга Cian.ru
├── 📊 Статистика
├── 📑 Содержание (с якорями)
└── Объявления
    ├── 💰 Цена
    ├── 📍 Адрес + 🚇 Метро
    ├── 📏 Площадь и характеристики
    ├── 📝 Полное описание
    ├── 🔍 Доп. характеристики
    ├── 🏘️ Похожие объявления
    ├── 📷 Изображения (с превью!)
    ├── 🗺️ Карта (Яндекс.Карты)
    └── 📞 Контакты
```

### ✨ Особенности

1. **Интерактивный**: кликабельные ссылки на объявления и карты
2. **Читаемый**: красивое форматирование с эмодзи
3. **Компактный**: длинные списки спрятаны в `<details>`
4. **Универсальный**: открывается везде (GitHub, VS Code, браузер)

## Просмотр

### В VS Code
```bash
# Откройте файл и нажмите:
Cmd+Shift+V (macOS)
Ctrl+Shift+V (Windows/Linux)
```

### В браузере
Многие Markdown редакторы имеют превью:
- [Typora](https://typora.io/)
- [MacDown](https://macdown.uranusjr.com/)
- [MarkdownPad](http://markdownpad.com/)

### В GitHub
Просто загрузите `.md` файл - автоматически отобразится с форматированием!

## Программное использование

```python
from src.cian_parser_playwright import CianParserPlaywright
from src.markdown_exporter import save_results_as_markdown

# Парсим
with CianParserPlaywright(headless=True) as parser:
    results = []
    for url in urls:
        data = parser.parse_detail_page(url)
        results.append(data)

# Экспортируем в Markdown
md_file = save_results_as_markdown(results, "my_results.md")
print(f"Сохранено: {md_file}")
```

## Конвертация

### В PDF
```bash
# Установите pandoc
brew install pandoc  # macOS
# или apt-get install pandoc  # Linux

# Конвертируйте
pandoc demo_export.md -o demo_export.pdf
```

### В HTML
```bash
pandoc demo_export.md -o demo_export.html
```

### В DOCX
```bash
pandoc demo_export.md -o demo_export.docx
```

## Полезные команды

### Поиск по файлу
```bash
# Найти объявления с определенным метро
grep -i "цветной бульвар" cian_results*.md

# Найти квартиры в ценовом диапазоне
grep -E "[0-9]+ млн ₽" cian_results*.md

# Найти по адресу
grep -i "проспект мира" cian_results*.md
```

### Статистика
```bash
# Сколько объявлений
grep -c "^## Объявление" cian_results*.md

# Сколько изображений всего
grep -c "^!\[Изображение\]" cian_results*.md
```

## Советы

### 💡 Изображения без водяных знаков
В Markdown файле есть инструкция для каждого объявления:
```
/images/ → /images-no-watermark/
```

### 💡 Шаринг результатов
Markdown идеален для отправки:
- Коллегам (просто файл)
- В Git (автоматический рендеринг)
- В документацию (часть проекта)
- Клиентам (конвертируйте в PDF)

### 💡 Архивирование
Markdown - это текст, поэтому:
- Отлично сжимается
- Легко индексируется
- Можно версионировать в Git
- Не устареет (в отличие от проприетарных форматов)

## Что дальше?

Читайте подробную документацию: [MARKDOWN_EXPORT.md](MARKDOWN_EXPORT.md)
