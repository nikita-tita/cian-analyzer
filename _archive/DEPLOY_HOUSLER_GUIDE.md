# 🚀 Инструкция по деплою Housler на production

## 📋 Обзор

Этот гайд описывает как задеплоить полный стек Housler (лендинг + блог + аналитика) на production сервер с доменом **housler.ru**.

---

## 🎯 Что будет развернуто

| Компонент | URL | Описание |
|-----------|-----|----------|
| **Лендинг** | `https://housler.ru/` | Главная страница агентства |
| **Блог** | `https://housler.ru/blog` | Статьи о недвижимости |
| **Калькулятор** | `https://housler.ru/calculator` | Анализ объектов недвижимости |
| **API** | `https://housler.ru/api/*` | REST API для аналитики |
| **Sitemap** | `https://housler.ru/sitemap.xml` | Карта сайта для SEO |

---

## 🔧 Требования

### На локальной машине:
- SSH доступ к серверу (root@91.229.8.221)
- `rsync` для копирования файлов
- Git репозиторий с актуальным кодом

### На production сервере:
- Ubuntu 20.04+ или Debian 11+
- Python 3.9+
- Nginx
- Certbot (для SSL)
- Systemd

---

## ⚡ Быстрый старт

### 1. Проверка DNS

Убедитесь что DNS записи для `housler.ru` указывают на сервер:

```bash
dig housler.ru +short
# Должно вернуть: 91.229.8.221
```

Если нет - добавьте A-запись:
```
Тип:  A
Имя:  @
Значение: 91.229.8.221
TTL:  3600
```

### 2. Проверка SSH ключей

```bash
# Проверьте подключение
ssh root@91.229.8.221 'echo "OK"'

# Если нужно - добавьте SSH ключ
ssh-copy-id root@91.229.8.221
```

### 3. Запуск деплоя

```bash
cd /Users/fatbookpro/Desktop/cian

# Сделайте скрипт исполняемым (уже сделано)
chmod +x deploy-housler-full.sh

# Запустите деплой
./deploy-housler-full.sh
```

Скрипт выполнит все шаги автоматически:
1. ✅ Проверка подключения к серверу
2. ✅ Копирование файлов
3. ✅ Установка зависимостей
4. ✅ Настройка .env
5. ✅ Создание systemd сервиса
6. ✅ Настройка Nginx
7. ✅ Получение SSL сертификата (Let's Encrypt)
8. ✅ Запуск приложения
9. ✅ Проверка работоспособности

---

## 📂 Структура на сервере

```
/var/www/housler/
├── app_new.py              # Основное Flask приложение
├── blog_routes.py          # Роуты блога
├── blog_database.py        # База данных блога
├── blog.db                 # SQLite БД блога
├── src/                    # Модули аналитики
├── templates/              # HTML шаблоны
│   ├── index.html         # Лендинг
│   ├── blog_index.html    # Список статей
│   └── blog_post.html     # Страница статьи
├── static/                 # Статические файлы
├── venv/                   # Виртуальное окружение
├── .env                    # Конфигурация
└── backups/               # Бэкапы БД

/etc/nginx/sites-available/
└── housler.ru             # Nginx конфигурация

/etc/systemd/system/
└── housler.service        # Systemd сервис

/var/log/housler/
├── app.log                # Логи приложения
└── error.log              # Логи ошибок
```

---

## 🔍 Проверка работы

### После деплоя проверьте:

1. **Лендинг:**
   ```bash
   curl -I https://housler.ru/
   # Ожидаем: HTTP/2 200
   ```

2. **Блог:**
   ```bash
   curl -I https://housler.ru/blog
   # Ожидаем: HTTP/2 200
   ```

3. **Калькулятор:**
   ```bash
   curl -I https://housler.ru/calculator
   # Ожидаем: HTTP/2 200
   ```

4. **Health check:**
   ```bash
   curl https://housler.ru/health
   # Ожидаем: {"status":"healthy",...}
   ```

5. **Sitemap:**
   ```bash
   curl -I https://housler.ru/sitemap.xml
   # Ожидаем: HTTP/2 200, Content-Type: application/xml
   ```

---

## 📊 Мониторинг

### Просмотр логов:

```bash
# Логи приложения
ssh root@91.229.8.221 'tail -f /var/log/housler/app.log'

# Логи Nginx
ssh root@91.229.8.221 'tail -f /var/log/nginx/housler.ru.access.log'

# Логи systemd
ssh root@91.229.8.221 'journalctl -u housler -f'
```

### Статус сервиса:

```bash
ssh root@91.229.8.221 'systemctl status housler'
```

### Метрики:

```bash
# Prometheus метрики (только с сервера)
ssh root@91.229.8.221 'curl http://localhost:5002/metrics'
```

---

## 🔧 Управление

### Рестарт приложения:

```bash
ssh root@91.229.8.221 'systemctl restart housler'
```

### Остановка:

```bash
ssh root@91.229.8.221 'systemctl stop housler'
```

### Просмотр конфигурации:

```bash
# Nginx конфигурация
ssh root@91.229.8.221 'cat /etc/nginx/sites-available/housler.ru'

# Systemd сервис
ssh root@91.229.8.221 'cat /etc/systemd/system/housler.service'

# .env файл
ssh root@91.229.8.221 'cat /var/www/housler/.env'
```

### Обновление кода:

```bash
# Просто запустите скрипт снова
./deploy-housler-full.sh

# Или вручную
ssh root@91.229.8.221 'cd /var/www/housler && git pull && systemctl restart housler'
```

---

## 🐛 Troubleshooting

### Приложение не запускается

```bash
# Проверьте логи
ssh root@91.229.8.221 'journalctl -u housler -n 50'

# Проверьте статус
ssh root@91.229.8.221 'systemctl status housler'

# Проверьте зависимости
ssh root@91.229.8.221 'cd /var/www/housler && source venv/bin/activate && pip list'
```

### Nginx возвращает 502 Bad Gateway

```bash
# Проверьте что приложение запущено
ssh root@91.229.8.221 'systemctl status housler'

# Проверьте что приложение слушает порт 5002
ssh root@91.229.8.221 'netstat -tlnp | grep 5002'

# Проверьте логи Nginx
ssh root@91.229.8.221 'tail -50 /var/log/nginx/housler.ru.error.log'
```

### SSL сертификат не получается

```bash
# Проверьте DNS
dig housler.ru +short

# Проверьте что порты 80 и 443 открыты
ssh root@91.229.8.221 'netstat -tlnp | grep -E ":80|:443"'

# Попробуйте получить сертификат вручную
ssh root@91.229.8.221 'certbot --nginx -d housler.ru -d www.housler.ru'
```

### Блог не работает

```bash
# Проверьте наличие БД
ssh root@91.229.8.221 'ls -lh /var/www/housler/blog.db'

# Проверьте права доступа
ssh root@91.229.8.221 'chmod 644 /var/www/housler/blog.db'

# Заново инициализируйте БД
ssh root@91.229.8.221 'cd /var/www/housler && python3 seed_blog.py'
```

---

## 🔐 Безопасность

### После деплоя рекомендуется:

1. **Настроить firewall:**
   ```bash
   ssh root@91.229.8.221 'ufw allow 80/tcp && ufw allow 443/tcp && ufw enable'
   ```

2. **Ограничить доступ к метрикам:**
   - Metrics endpoint (`/metrics`) уже ограничен только localhost в Nginx

3. **Настроить автоматическое обновление SSL:**
   ```bash
   ssh root@91.229.8.221 'systemctl enable certbot.timer'
   ```

4. **Настроить регулярные бэкапы:**
   ```bash
   # Создайте cronjob для бэкапа БД
   ssh root@91.229.8.221 'crontab -e'
   # Добавьте: 0 2 * * * sqlite3 /var/www/housler/blog.db ".backup /var/www/housler/backups/blog_$(date +\%Y\%m\%d).db"
   ```

---

## 📈 Performance

### Оптимизация после деплоя:

1. **Redis для кэширования** (если еще не настроен):
   ```bash
   ssh root@91.229.8.221 'apt install redis-server'
   # Обновите .env: REDIS_ENABLED=true
   ```

2. **Gunicorn для production** (вместо Flask dev server):
   - Обновите systemd сервис для использования Gunicorn

3. **Мониторинг через Prometheus/Grafana** (опционально)

---

## 📞 Поддержка

Если что-то пошло не так:

1. Проверьте раздел [Troubleshooting](#-troubleshooting)
2. Изучите логи приложения и Nginx
3. Проверьте статус всех сервисов
4. Убедитесь что DNS записи правильные
5. Проверьте что SSL сертификат действителен

---

## ✅ Чеклист после деплоя

- [ ] Лендинг открывается: https://housler.ru/
- [ ] Блог работает: https://housler.ru/blog
- [ ] Калькулятор доступен: https://housler.ru/calculator
- [ ] SSL сертификат установлен (HTTPS)
- [ ] Health check отвечает: https://housler.ru/health
- [ ] Sitemap генерируется: https://housler.ru/sitemap.xml
- [ ] Логи пишутся корректно
- [ ] Systemd сервис включен для автозапуска
- [ ] Firewall настроен
- [ ] Бэкапы настроены

---

**Готово! 🎉**

Ваш проект Housler успешно развернут на production сервере!
