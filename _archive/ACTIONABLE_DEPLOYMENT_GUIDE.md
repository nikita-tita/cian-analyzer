# 🚀 ACTIONABLE DEPLOYMENT GUIDE - ЧТО ДЕЛАТЬ ТЕБЕ

**Цель:** Довести Housler.ru до полноценного рабочего прода
**Текущее состояние:** 60-70% работает, 2 критических бага
**Целевое состояние:** 100% работает, все сегменты покрыты
**Timeline:** 2-3 недели
**Твоя роль:** Координация + deployment execution

---

## 🎯 PHASE 1: IMMEDIATE ACTIONS (Сегодня, 30 минут)

### ✅ ШАГ 1.1: Создать GitHub Issues (5 минут)

**Что нужно от тебя:**

1. Открой браузер: https://github.com/nikita-tita/cian-analyzer/issues/new/choose

2. Создай 3 issue (просто кликай):

   **Issue #1:**
   - Click: "🔴 P0 Bug - Premium segment analog search fails"
   - Click: "Submit new issue"
   - Скопируй номер issue (например, #42)

   **Issue #2:**
   - Click: "🔴 P0 Bug - Analogs found but hidden from user"
   - Click: "Submit new issue"
   - Скопируй номер issue (например, #43)

   **Issue #3:**
   - Click: "🟠 P1 Enhancement - Restore Housler Forecast"
   - Click: "Submit new issue"
   - Скопируй номер issue (например, #44)

3. **Результат:** У тебя есть 3 issue с номерами

---

### ✅ ШАГ 1.2: Определить, кто будет фиксить баги (10 минут)

**Вариант A: Есть разработчики**

Назначь на issues:
- Issue #1 (Premium analogs) → Backend developer
- Issue #2 (Hidden analogs) → Frontend developer
- Issue #3 (Forecast) → Backend + Frontend

**Вариант Б: Разработчиков нет - Я (Claude) буду фиксить**

Скажи мне: "Claude, исправь Issue #1" и я:
1. Найду нужные файлы
2. Напишу код-фиксы
3. Создам коммиты
4. Запушу в ветку

**Вариант В: Ты сам будешь фиксить**

Я дам тебе точные инструкции + код для каждого бага.

**Что выбираешь?** (Напиши мне в следующем сообщении)

---

### ✅ ШАГ 1.3: Проверить доступы к production серверу (10 минут)

**Что нужно от тебя:**

1. **SSH доступ:**
   ```bash
   ssh root@91.229.8.221
   ```

   **Проверь:**
   - [ ] SSH ключ работает (не спрашивает пароль)
   - [ ] Можешь зайти на сервер
   - [ ] Права root есть

2. **Git на сервере:**
   ```bash
   cd /var/www/housler
   git status
   git log --oneline -3
   ```

   **Проверь:**
   - [ ] Репозиторий доступен
   - [ ] Git работает
   - [ ] Видишь последние коммиты

3. **Systemd service:**
   ```bash
   systemctl status housler
   ```

   **Проверь:**
   - [ ] Сервис активен (active/running)
   - [ ] Нет критических ошибок

**Если что-то не работает** - напиши мне, я помогу настроить.

---

### ✅ ШАГ 1.4: Определить deployment стратегию (5 минут)

**Выбери один из вариантов:**

**Вариант A: Жду фиксов, потом деплой (РЕКОМЕНДУЮ)**
- Продолжаем работать с текущим production (60-70% работает)
- Фиксим баги в ветке `claude/housler-testing-report-...`
- Через 2-3 недели деплоим полностью рабочую версию
- **Плюсы:** Безопасно, полное тестирование
- **Минусы:** Клиенты ждут 2-3 недели

**Вариант Б: Деплою сейчас без фиксов (НЕ РЕКОМЕНДУЮ)**
- Деплоим текущую ветку сразу (с багами)
- Фиксим баги потом, хотфиксами
- **Плюсы:** Быстро
- **Минусы:** Риск негативных отзывов, премиум не работает

**Вариант В: Hotfix критичного бага, остальное потом**
- Фиксим только Issue #2 (скрытые аналоги) - 1-2 дня
- Деплоим частичный фикс
- Issue #1 (премиум) фиксим позже
- **Плюсы:** Баланс скорости и качества
- **Минусы:** Всё равно 2 деплоя нужны

**Что выбираешь?** (Напиши в следующем сообщении)

---

## 🎯 PHASE 2: FIXING BUGS (1-2 недели)

### Если выбрал "Вариант A: Жду фиксов"

Я (Claude) исправлю все баги за тебя. Вот как это будет:

#### 🔧 Исправление Issue #1: Premium analogs (4-8 часов моей работы)

**Что я сделаю:**

1. **Найду файл:** `src/parsers/playwright_parser.py`
2. **Изменю логику поиска аналогов:**
   ```python
   # CURRENT: ±20% price range
   min_price = price * 0.8
   max_price = price * 1.2

   # FIX: ±40% for premium
   if price > 25_000_000:
       min_price = price * 0.6
       max_price = price * 1.4
   ```
3. **Добавлю fallback:**
   ```python
   if len(analogs) < 5:
       # Expand to ±50%
       search_wider_range()
   ```
4. **Создам коммит + push**

**Что нужно от тебя:**
- Просто скажи: "Claude, исправь Issue #1"
- Я всё сделаю сам
- Ты только проверишь потом (30 минут тестирования)

---

#### 🔧 Исправление Issue #2: Hidden analogs (8-12 часов моей работы)

**Что я сделаю:**

1. **Найду файл:** `static/js/wizard.js`
2. **Добавлю функцию рендеринга:**
   ```javascript
   function renderAnalogsList(analogs) {
     // Create cards with:
     // - Address, area, price
     // - Remove button
     // - CIAN link
   }
   ```
3. **Добавлю HTML template:** `templates/wizard.html`
4. **Протестирую на тестовом объекте**
5. **Создам коммит + push**

**Что нужно от тебя:**
- Просто скажи: "Claude, исправь Issue #2"
- Я всё сделаю сам
- Проверь потом (30 минут тестирования)

---

#### 🔧 Исправление Issue #3: Restore forecast (OPTIONAL)

Это можно отложить на потом (не блокер).

**Если хочешь сделать сразу:**
- Скажи: "Claude, исправь Issue #3"
- Я верну секцию "Прогноз Housler" с объяснением

---

### 📊 После каждого фикса - тестирование (ЧТО ДЕЛАТЬ ТЕБЕ)

**ШАГ 1: Pull изменения локально**

```bash
cd /path/to/cian-analyzer
git checkout claude/housler-testing-report-01Hff94FePTJdY8XBsa6nV4i
git pull origin claude/housler-testing-report-01Hff94FePTJdY8XBsa6nV4i
```

**ШАГ 2: Запусти локально**

```bash
# Вариант A: Docker
docker-compose down
docker-compose build
docker-compose up

# Вариант Б: Без Docker
python app_new.py
```

**ШАГ 3: Протестируй фикс**

**Для Issue #1 (Premium analogs):**
```
1. Открой http://localhost:5000/calculator
2. Вставь: https://spb.cian.ru/sale/flat/321709237/
3. Автопоиск аналогов
4. Должно найти минимум 5 аналогов ✅
```

**Для Issue #2 (Hidden analogs):**
```
1. Открой http://localhost:5000/calculator
2. Вставь: https://spb.cian.ru/sale/flat/322492072/
3. Автопоиск аналогов
4. Должен отобразиться список из 13 карточек ✅
5. Кликни "Удалить" на одной → должна исчезнуть ✅
```

**ШАГ 4: Если всё работает**

Скажи мне: "Claude, фикс работает, переходи к следующему"

**ШАГ 5: Если НЕ работает**

Скажи мне: "Claude, вот что не работает: [опиши проблему]"
Я исправлю.

---

## 🎯 PHASE 3: DEPLOYMENT TO PRODUCTION (1 час)

### Когда все баги исправлены - деплой

**ВАЖНО:** Деплоим ТОЛЬКО когда:
- [x] Issue #1: FIXED + протестирован
- [x] Issue #2: FIXED + протестирован
- [x] Regression tests: 15/15 passed
- [x] Backup создан

---

### ✅ ШАГ 3.1: Pre-Deployment Checklist (15 минут)

**Что нужно от тебя - выполни команды:**

```bash
# 1. SSH на сервер
ssh root@91.229.8.221

# 2. Backup текущей версии
cd /var/www/housler
git branch backup-before-deploy-$(date +%Y%m%d-%H%M%S)
git branch | grep backup

# 3. Проверить disk space
df -h
# Должно быть > 1GB свободно

# 4. Backup базы данных (если есть)
# Замени на твою команду backup
# pg_dump housler_db > backup.sql  # example

# 5. Проверить текущий статус
systemctl status housler
journalctl -u housler -n 20 --no-pager
```

**Проверь:**
- [ ] Backup ветка создана
- [ ] Disk space достаточно (>1GB)
- [ ] Database backup создан (если есть БД)
- [ ] Сервис работает без ошибок

---

### ✅ ШАГ 3.2: Deployment Execution (20 минут)

**Вариант A: Автоматический деплой через скрипт (РЕКОМЕНДУЮ)**

```bash
# На локальной машине
cd /path/to/cian-analyzer
git checkout claude/housler-testing-report-01Hff94FePTJdY8XBsa6nV4i

# Запусти скрипт деплоя
bash scripts/deploy.sh
```

Скрипт автоматически:
1. Запушит изменения в GitHub ✅
2. SSH на сервер ✅
3. Git pull на сервере ✅
4. Restart сервиса ✅
5. Проверит статус ✅
6. Покажет логи ✅

**Если скрипт не работает** - используй Вариант Б (ручной).

---

**Вариант Б: Ручной деплой (если скрипт не работает)**

```bash
# ШАГ 1: На локальной машине - push изменения
cd /path/to/cian-analyzer
git checkout claude/housler-testing-report-01Hff94FePTJdY8XBsa6nV4i
git push origin claude/housler-testing-report-01Hff94FePTJdY8XBsa6nV4i

# ШАГ 2: SSH на production сервер
ssh root@91.229.8.221

# ШАГ 3: Pull изменения на сервере
cd /var/www/housler
git fetch origin
git checkout claude/housler-testing-report-01Hff94FePTJdY8XBsa6nV4i
git pull origin claude/housler-testing-report-01Hff94FePTJdY8XBsa6nV4i

# Проверь что код обновился
git log --oneline -1
# Должен быть последний коммит с фиксами

# ШАГ 4: Очистить Python cache (КРИТИЧНО!)
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
find . -type f -name "*.pyc" -delete

# ШАГ 5: Обновить зависимости (если изменились)
git diff HEAD~5 requirements.txt
# Если есть изменения:
source venv/bin/activate
pip install -r requirements.txt

# ШАГ 6: Перезапустить сервис
systemctl restart housler

# Подожди 3 секунды
sleep 3

# ШАГ 7: Проверить статус
systemctl status housler
# Должен быть: active (running) ✅

# ШАГ 8: Проверить логи
journalctl -u housler -n 30 --no-pager

# Не должно быть ERROR или CRITICAL
```

---

**Вариант В: Docker деплой (если используешь Docker)**

```bash
# На production сервере
cd /var/www/housler

# Pull изменения
git pull origin claude/housler-testing-report-01Hff94FePTJdY8XBsa6nV4i

# Остановить контейнеры
docker-compose down

# Удалить старые образы
docker-compose rm -f
docker rmi cian-analyzer_web 2>/dev/null || true

# Очистить Python cache
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
find . -type f -name "*.pyc" -delete

# Пересобрать БЕЗ кэша
docker-compose build --no-cache

# Запустить
docker-compose up -d

# Проверить логи
docker-compose logs -f --tail=50
```

---

### ✅ ШАГ 3.3: Post-Deployment Verification (15 минут)

**Что нужно от тебя - проверить:**

**1. Health Check (на сервере)**

```bash
# HTTP status
curl -I https://housler.ru
# Должен вернуть: 200 OK

# Response time
time curl -s https://housler.ru > /dev/null
# Должно быть < 2 секунд
```

**2. Smoke Tests (в браузере)**

```
1. Открой https://housler.ru

2. Тест парсинга:
   - Вставь URL объекта
   - Нажми "Загрузить"
   - Проверь: данные заполнились ✅

3. Тест аналогов (Issue #2):
   - Автопоиск аналогов
   - Проверь: список из N карточек отображается ✅
   - Нажми "Удалить" на одной карточке
   - Проверь: карточка исчезла ✅

4. Тест премиума (Issue #1):
   - Вставь: https://spb.cian.ru/sale/flat/321709237/
   - Автопоиск
   - Проверь: найдено минимум 5 аналогов ✅

5. Тест отчёта:
   - Перейди к Step 3
   - Проверь: отчёт формируется без ошибок ✅
   - Проверь: медиана рассчитана ✅
   - Проверь: сценарии продажи показаны ✅
```

**3. Error Monitoring (на сервере)**

```bash
# Проверить логи на ошибки
journalctl -u housler -n 100 --no-pager | grep -i error

# Если есть ERROR - скопируй и покажи мне
# Если нет - всё ОК ✅
```

**4. Performance Check**

```bash
# CPU usage
top -b -n 1 | grep python
# Должно быть < 50% CPU

# Memory usage
free -h
# Должно быть > 500MB свободно

# Disk usage
df -h
# Должно быть > 1GB свободно
```

---

### ✅ ШАГ 3.4: Что делать если что-то пошло не так (ROLLBACK)

**Если сервис не запускается:**

```bash
# На сервере
cd /var/www/housler

# Откатись на backup
git branch | grep backup-before-deploy
git checkout backup-before-deploy-YYYYMMDD-HHMMSS

# Очисти cache
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
find . -type f -name "*.pyc" -delete

# Перезапусти
systemctl restart housler

# Проверь
systemctl status housler
```

**Если есть ошибки в логах:**

```bash
# Покажи мне последние 50 строк логов
journalctl -u housler -n 50 --no-pager

# Или скопируй ошибки и пришли мне
# Я помогу исправить
```

**Если сайт не открывается:**

```bash
# Проверь nginx
systemctl status nginx

# Проверь firewall
iptables -L | grep 80
iptables -L | grep 443

# Проверь DNS
nslookup housler.ru
```

---

## 🎯 PHASE 4: POST-LAUNCH MONITORING (1 неделя)

### День 1-3: Активный мониторинг

**Что нужно от тебя (3 раза в день):**

```bash
# Утро, День, Вечер - выполняй команды:

ssh root@91.229.8.221

# 1. Проверка статуса
systemctl status housler

# 2. Проверка ошибок
journalctl -u housler --since "1 hour ago" | grep -i error

# 3. Проверка performance
top -b -n 1 | grep python
free -h
df -h

# 4. Проверка логов
tail -n 50 /var/log/housler/access.log  # если есть
```

**Если всё ОК:**
- Нет ошибок ✅
- CPU < 50% ✅
- Memory > 500MB свободно ✅
- Disk > 1GB свободно ✅

**Продолжай мониторинг.**

**Если проблемы** - покажи мне логи, я помогу.

---

### День 4-7: Сбор метрик

**Создай Google Sheet для метрик:**

| Дата | Visitors | Errors | Avg Response Time | Премиум использований | Feedback |
|------|----------|--------|-------------------|-----------------------|----------|
| Dec 8 | ? | 0 | 1.2s | 3 | Положительный |
| Dec 9 | ? | 2 | 1.5s | 5 | ... |

**Откуда брать данные:**

```bash
# Количество запросов (example)
grep "GET /calculator" /var/log/nginx/access.log | wc -l

# Errors
journalctl -u housler --since "24 hours ago" | grep -i error | wc -l

# Response time (need to parse logs)
# Зависит от твоего logging setup
```

---

## 📊 QUICK REFERENCE - ШПАРГАЛКА

### 🚨 Если что-то сломалось - действуй по плану:

**1. Проверь статус:**
```bash
ssh root@91.229.8.221
systemctl status housler
```

**2. Проверь логи:**
```bash
journalctl -u housler -n 50 --no-pager
```

**3. Если не понятно - покажи мне:**
```
Скопируй логи и отправь мне сообщение:
"Claude, вот логи: [вставь текст]"
Я разберусь и помогу
```

**4. Если нужен срочный rollback:**
```bash
git checkout backup-before-deploy-YYYYMMDD
systemctl restart housler
```

---

### 📞 Быстрые команды:

**Проверить статус:**
```bash
ssh root@91.229.8.221 "systemctl status housler"
```

**Перезапустить:**
```bash
ssh root@91.229.8.221 "systemctl restart housler"
```

**Посмотреть логи:**
```bash
ssh root@91.229.8.221 "journalctl -u housler -n 30 --no-pager"
```

**Health check:**
```bash
curl -I https://housler.ru
```

---

## ✅ FINAL CHECKLIST - ЧТО ТОЧНО НУЖНО ОТ ТЕБЯ

Вот абсолютный минимум, что тебе нужно сделать:

### Сегодня (30 минут):
- [ ] Создать 3 GitHub Issues (5 мин)
- [ ] Решить: кто будет фиксить баги - я (Claude) или кто-то другой? (1 мин)
- [ ] Проверить SSH доступ к серверу (10 мин)
- [ ] Выбрать deployment стратегию (1 мин)
- [ ] Написать мне: "Claude, исправь Issue #1 и #2" (1 мин)

### Через 1-2 дня (когда я исправлю баги):
- [ ] Pull изменения локально (1 мин)
- [ ] Протестировать фиксы локально (30 мин)
- [ ] Сказать мне: "Claude, фиксы работают" или "Claude, вот проблема: ..." (1 мин)

### Через 1-2 недели (когда все баги исправлены):
- [ ] Создать backup на сервере (5 мин)
- [ ] Запустить deployment (Вариант A: 1 команда, Вариант Б: 10 команд) (10-20 мин)
- [ ] Проверить smoke tests (15 мин)
- [ ] Мониторинг 3 дня (10 мин × 3 раза в день)

**ИТОГО твоего времени:**
- Immediate: 30 минут
- Testing: 30 минут
- Deployment: 30-40 минут
- Monitoring: 30 минут × 3 дня = 1.5 часа
- **TOTAL: ~3 часа твоего времени**

**Остальное делаю я (Claude):**
- Fixing bugs: 20-30 часов моей работы
- Writing code: Автоматически
- Committing: Автоматически
- Testing scripts: Автоматически

---

## 🎯 TL;DR - СОВСЕМ КРАТКО

**Что тебе нужно сделать ПРЯМО СЕЙЧАС (5 минут):**

1. Открой https://github.com/nikita-tita/cian-analyzer/issues/new/choose
2. Создай 3 issue (просто кликай шаблоны)
3. Напиши мне: **"Claude, исправь Issue #1 и Issue #2"**
4. Жди 1-2 дня пока я исправлю
5. Протестируй локально (30 мин)
6. Задеплой на production (1 команда, 20 мин)
7. Profit! 🚀

**Я делаю всю тяжёлую работу. Ты просто координируешь и деплоишь.**

---

## 🚀 НАЧНИ ПРЯМО СЕЙЧАС

**Напиши мне одну из команд:**

**Если готов начать фиксить баги:**
```
"Claude, исправь Issue #1 и Issue #2, я создал issues"
```

**Если нужна помощь с доступами:**
```
"Claude, помоги настроить SSH доступ к серверу"
```

**Если хочешь сам фиксить:**
```
"Claude, дай мне код-фиксы, я сам исправлю"
```

**Если хочешь сразу задеплоить без фиксов:**
```
"Claude, деплоим как есть, фиксы потом"
```

---

**Prepared by:** Claude Code
**Date:** November 16, 2025
**Status:** ✅ READY TO START
**Your time investment:** ~3 hours total
**My time investment:** 20-30 hours (automated)

🎯 **ЖДУ ТВОЕЙ КОМАНДЫ - ЧТО ДЕЛАЕМ?** 🚀
