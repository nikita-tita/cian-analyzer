# 📰 Ручной запуск парсера блога

## 🎯 Назначение

Скрипт **parse-blog-now.sh** позволяет запустить парсинг статей с CIAN Magazine **в любое время**, не дожидаясь автоматического запуска в 10:00.

---

## 🚀 Как использовать

### Быстрый запуск

```bash
cd /Users/fatbookpro/Desktop/cian

./parse-blog-now.sh
```

### Что произойдёт:

1. **Скрипт спросит** сколько статей распарсить (1-10)
2. **Подключится** к серверу `91.229.8.221`
3. **Проверит** сколько сейчас статей в базе
4. **Запустит** парсер с нужным количеством
5. **Покажет** статистику и ссылки на новые статьи

---

## 📊 Интерактивное меню

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  📰 Ручной запуск Blog Parser
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Сколько статей распарсить?
  1 - Одна статья (быстро, для теста)
  3 - Три статьи (стандартно)
  5 - Пять статей
  10 - Десять статей (долго, ~10-15 минут)

Введите число [1-10]: _
```

---

## ⏱️ Время выполнения

| Статей | Примерное время |
|--------|----------------|
| 1 статья | ~2-3 минуты |
| 3 статьи | ~5-8 минут |
| 5 статей | ~8-12 минут |
| 10 статей | ~15-20 минут |

**Почему долго?**
- Playwright открывает браузер и парсит каждую статью
- Yandex GPT рерайтит контент (занимает 30-60 секунд на статью)
- Проверка на дубликаты в базе данных

---

## ✅ Проверка на дубликаты

**ДА!** Скрипт автоматически проверяет:

1. **Создаётся slug** из заголовка статьи
   - Например: "Как купить квартиру" → `kak-kupit-kvartiru`

2. **Проверяется в базе** через `db.post_exists(slug)`
   - Если slug уже есть → статья пропускается
   - Если slug новый → статья парсится и публикуется

3. **Вывод в консоль:**
   ```
   Article already exists: kak-kupit-kvartiru  ← пропущена
   Processing: Новая интересная статья...      ← будет добавлена
   ```

**Код проверки** ([blog_cli.py:46-48](blog_cli.py#L46-L48)):
```python
if db.post_exists(slug) and not force:
    logger.info(f"Article already exists: {slug}")
    continue
```

---

## 📖 Примеры использования

### Пример 1: Тестовый запуск (1 статья)

```bash
./parse-blog-now.sh
# Введите: 1
# Подождите 2-3 минуты
# Проверьте https://housler.ru/blog
```

---

### Пример 2: Стандартный запуск (3 статьи)

```bash
./parse-blog-now.sh
# Введите: 3
# Подождите 5-8 минут
# Проверьте https://housler.ru/blog
```

---

### Пример 3: Массовый парсинг (10 статей)

```bash
./parse-blog-now.sh
# Введите: 10
# Подождите 15-20 минут
# Проверьте https://housler.ru/blog
```

**Используйте для:** первоначального наполнения блога

---

## 📊 Что показывает скрипт

### Во время выполнения:

```
[1/3] Подключение к серверу...
✅ Подключение установлено

[2/3] Проверка текущих статей в базе...
✅ Сейчас в базе: 10 статей

[3/3] Запуск парсера...
⏳ Это может занять 2-5 минут на статью...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 Начинаю парсинг 3 статей...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Starting to parse 3 articles from CIAN magazine...
Found 15 articles
Processing: Как выбрать квартиру в новостройке...
Rewriting article with Yandex GPT...
✓ Published: Как правильно выбрать квартиру в новостройке (ID: 11)

Processing: Ипотека в 2025 году: что изменилось...
Rewriting article with Yandex GPT...
✓ Published: Ипотека в 2025: полный гид для покупателей (ID: 12)

Article already exists: investitsii-v-nedvizhimost  ← пропущена!

Processing: Риски при покупке вторичного жилья...
Rewriting article with Yandex GPT...
✓ Published: Покупка вторички: как избежать рисков (ID: 13)

Done! Published 3 articles

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Итоговая статистика:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Было статей: 10
Стало статей: 13
Добавлено новых: 3

✅ Последние добавленные статьи:
1. Как правильно выбрать квартиру в новостройке
   URL: https://housler.ru/blog/kak-pravilno-vybrat-kvartiru-v-novostrojke

2. Ипотека в 2025: полный гид для покупателей
   URL: https://housler.ru/blog/ipoteka-v-2025-polnyj-gid-dlya-pokupatelej

3. Покупка вторички: как избежать рисков
   URL: https://housler.ru/blog/pokupka-vtorichki-kak-izbezhat-riskov
```

### После завершения:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Парсинг завершён успешно!

📝 Проверить статьи на сайте:
   https://housler.ru/blog

📊 Посмотреть логи:
   ssh -i ~/.ssh/id_housler root@91.229.8.221 'tail -50 /var/log/housler/blog_parser_cron.log'
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🔧 Технические детали

### Что делает скрипт:

1. **Подключается по SSH** к серверу `91.229.8.221`
2. **Проверяет** текущее количество статей в `blog.db`
3. **Запускает** `blog_cli.py parse -n N` на сервере
4. **Парсит** CIAN Magazine через Playwright
5. **Рерайтит** статьи через Yandex GPT API
6. **Публикует** в базу данных
7. **Показывает** статистику и ссылки

### Используемые файлы на сервере:

```
/var/www/housler/
├── blog_cli.py              ← CLI для управления блогом
├── blog_parser_playwright.py ← Парсер CIAN Magazine
├── yandex_gpt.py            ← Интеграция с Yandex GPT
├── blog_database.py         ← Работа с БД
├── blog.db                  ← SQLite база статей
└── venv/                    ← Python виртуальное окружение
```

---

## 🆘 Troubleshooting

### Проблема: SSH не работает

**Ошибка:**
```
❌ Не удалось подключиться к серверу
Проверьте SSH ключ: /Users/fatbookpro/.ssh/id_housler
```

**Решение:**
```bash
# Проверьте что ключ существует
ls -la ~/.ssh/id_housler

# Если нет - создайте и скопируйте на сервер
ssh-keygen -t ed25519 -f ~/.ssh/id_housler -C "housler-deploy"
ssh-copy-id -i ~/.ssh/id_housler.pub root@91.229.8.221
```

---

### Проблема: Парсер падает с ошибкой

**Ошибка:**
```
❌ Ошибка при парсинге
```

**Решение:**
```bash
# Посмотрите логи для деталей
ssh -i ~/.ssh/id_housler root@91.229.8.221 'tail -100 /var/log/housler/blog_parser_cron.log'

# Частые причины:
# 1. Playwright не установлен
# 2. Yandex GPT API key не работает
# 3. База данных недоступна
```

---

### Проблема: Новых статей не добавлено

**Сообщение:**
```
ℹ️  Новых статей не добавлено (все уже были в базе)
```

**Причина:** Все статьи с CIAN Magazine уже были распарсены ранее

**Решение:**
```bash
# Вариант 1: Подождите несколько дней, пока на CIAN выйдут новые статьи

# Вариант 2: Используйте альтернативный источник (RBC Realty)
ssh -i ~/.ssh/id_housler root@91.229.8.221 'cd /var/www/housler && source venv/bin/activate && python3 auto_blog_daemon.py --source rbc'

# Вариант 3: Принудительно перепарсить (с --force флагом)
# ⚠️ Осторожно: создаст дубликаты!
ssh -i ~/.ssh/id_housler root@91.229.8.221 'cd /var/www/housler && source venv/bin/activate && python3 blog_cli.py parse -n 3 --force'
```

---

## 🎯 FAQ

### Q: Можно ли запустить парсинг с локального компьютера?

**A:** Нет, парсинг всегда выполняется на сервере `91.229.8.221`. Этот скрипт просто подключается по SSH и запускает команду на сервере.

---

### Q: Сколько статей парсить каждый день автоматически?

**A:** По умолчанию **10 статей** каждый день в 10:00 (настроено в cron job).

Чтобы изменить:
```bash
# Подключитесь к серверу
ssh -i ~/.ssh/id_housler root@91.229.8.221

# Отредактируйте скрипт
nano /var/www/housler/cron_parse_blog.sh

# Измените число:
python3 blog_cli.py parse -n 20  # ← изменить на 20 статей
```

**Альтернативные источники:**
Если CIAN Magazine не дает достаточно новых статей, можно использовать RBC Realty:
```bash
ssh -i ~/.ssh/id_housler root@91.229.8.221 'cd /var/www/housler && source venv/bin/activate && python3 auto_blog_daemon.py --source rbc'
```

---

### Q: Как часто можно запускать парсинг вручную?

**A:** Сколько угодно раз. Но учтите:
- Парсинг нагружает сервер (Playwright + Yandex GPT)
- CIAN может заблокировать IP при слишком частых запросах
- **Рекомендация:** не чаще 1 раза в час

---

### Q: Что если парсер нашёл только 1 новую статью из 10 запрошенных?

**A:** Это нормально! Парсер:
1. Находит N последних статей на CIAN
2. Проверяет какие уже есть в базе
3. Добавляет только новые
4. Если новых меньше чем запрошено - добавит столько, сколько нашёл

---

### Q: Можно ли удалить дубликаты из базы?

**A:** Да, но проще не допускать их:
```bash
# Посмотреть все статьи
ssh -i ~/.ssh/id_housler root@91.229.8.221 'cd /var/www/housler && source venv/bin/activate && python3 blog_cli.py list'

# Удалить конкретную статью по ID (через SQL)
ssh -i ~/.ssh/id_housler root@91.229.8.221 'cd /var/www/housler && sqlite3 blog.db "DELETE FROM blog_posts WHERE id = 15;"'
```

---

## 📚 Связанные документы

- [FINAL_DEPLOYMENT_GUIDE.md](FINAL_DEPLOYMENT_GUIDE.md) - Полная инструкция по деплою
- [BLOG_PARSER_FIX.md](BLOG_PARSER_FIX.md) - Фикс автоматического парсинга
- [blog_cli.py](blog_cli.py) - CLI для управления блогом

---

## ✅ Итого

**parse-blog-now.sh** - это удобный способ:
- ✅ Запустить парсинг **в любое время** (не ждать 10:00)
- ✅ Выбрать **сколько статей** парсить (1-10)
- ✅ Увидеть **статистику** и ссылки на новые статьи
- ✅ Проверить работу парсера **перед деплоем**

**Используйте для:**
- Тестирования парсера
- Быстрого добавления контента
- Наполнения блога перед запуском
- Срочной публикации новых статей

---

**Контакт:** hello@housler.ru
