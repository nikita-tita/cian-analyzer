# Плавающие кнопки навигации

## Что сделано

✅ Добавлена плавающая панель с кнопками, закрепленная к низу экрана

### Изменения

**1. HTML** ([dashboard_unified.html](src/templates/dashboard_unified.html))
- Добавлена плавающая панель `<div class="floating-action-bar">`
- Кнопка "Анализировать" теперь находится в плавающей панели
- Использует `form="analysisForm"` для связи с формой

**2. CSS** ([unified-dashboard.css](src/static/css/unified-dashboard.css))
- Новый класс `.floating-action-bar` - фиксированная панель внизу экрана
- Класс `.btn-floating` - стилизация кнопок в панели
- Автоматическое добавление `padding-bottom: 90px` к body, чтобы контент не перекрывался
- Responsive дизайн для мобильных устройств
- Плавные анимации при наведении и клике

### Характеристики

**Desktop:**
- Панель внизу экрана, белый фон с тенью
- Кнопки по центру
- Минимальная ширина кнопки: 200px
- Эффект поднятия при наведении

**Mobile:**
- Кнопки растянуты на всю ширину
- Вертикальная компоновка при нескольких кнопках
- Увеличенный padding для контента (140px)

### Стили кнопок

```css
.btn-primary.btn-floating {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
}
```

### Дополнительные возможности

Панель можно скрыть/показать программно:
```javascript
document.querySelector('.floating-action-bar').classList.add('hidden');
document.querySelector('.floating-action-bar').classList.remove('hidden');
```

## Следующие шаги

Если нужно добавить больше кнопок (например, "Назад", "Далее"), просто добавьте их внутрь `.floating-action-bar`:

```html
<div class="floating-action-bar">
    <button type="button" class="btn btn-secondary btn-floating">
        ← Назад
    </button>
    <button type="submit" form="analysisForm" class="btn btn-primary btn-floating">
        🔍 Анализировать
    </button>
    <button type="button" class="btn btn-secondary btn-floating">
        Далее →
    </button>
</div>
```

## Дата
2025-11-06
