# 🚀 Деплой Housler на Reg.ru

## Быстрая установка (одна команда)

Подключитесь к серверу и выполните:

```bash
ssh root@91.229.8.221
```

Затем скопируйте и выполните эту команду:

```bash
curl -fsSL https://raw.githubusercontent.com/nikita-tita/cian-analyzer/main/deploy_vps.sh | bash
```

## Что делает скрипт:

1. ✅ Обновляет систему (Ubuntu 22.04)
2. ✅ Устанавливает Python 3.11, Nginx, Redis
3. ✅ Устанавливает зависимости для Playwright
4. ✅ Клонирует проект из GitHub
5. ✅ Настраивает виртуальное окружение
6. ✅ Создает systemd сервис для автозапуска
7. ✅ Настраивает Nginx reverse proxy
8. ✅ Получает SSL сертификат (Let's Encrypt)

## После установки:

Сайт будет доступен по адресу: **https://housler.ru**

### Полезные команды:

```bash
# Проверить статус приложения
sudo systemctl status housler

# Перезапустить приложение
sudo systemctl restart housler

# Посмотреть логи
sudo journalctl -u housler -f

# Обновить проект
cd /var/www/housler
sudo -u housler git pull
sudo systemctl restart housler
```

## Настройка DNS

В панели Reg.ru добавьте A-запись:

```
Тип: A
Имя: @
Значение: IP_вашего_сервера
TTL: 3600
```

И для www:

```
Тип: A
Имя: www
Значение: IP_вашего_сервера
TTL: 3600
```

IP сервера будет показан в конце установки.

## Альтернатива: Настройка SSH ключа для автодеплоя

Если хотите автоматизировать обновления, добавьте этот ключ на сервер:

```bash
# На сервере выполните:
mkdir -p ~/.ssh
echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPGWTMcGKY9q7vnCEHC9HLuBO5EiaiPnESQIU9TFZ/MI housler-deploy" >> ~/.ssh/authorized_keys
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

После этого можно будет деплоить автоматически с локального компьютера.

## Решение проблем

### SSL сертификат не установился

```bash
# Проверьте DNS
dig housler.ru

# Попробуйте получить сертификат вручную
sudo certbot --nginx -d housler.ru -d www.housler.ru
```

### Приложение не запускается

```bash
# Проверьте логи
sudo journalctl -u housler -n 50

# Проверьте зависимости
cd /var/www/housler
source venv/bin/activate
pip install -r requirements.txt
```

### Nginx показывает ошибку

```bash
# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
