# 📦 Скачивание фото с удалением водяных знаков

## Что добавлено

В веб-интерфейс добавлена возможность скачивать все фотографии из парсинга одним архивом с автоматическим удалением водяных знаков!

## 🎯 Как использовать

1. **Откройте веб-интерфейс:**
   ```bash
   cd /Users/fatbookpro/Desktop/cian/webapp
   source ../venv/bin/activate
   python app_cian_style.py
   ```
   Откройте: **http://127.0.0.1:5002**

2. **Выполните парсинг:**
   - Вставьте ссылку на объявление Cian.ru
   - Нажмите "Проанализировать"
   - Подождите ~1-2 минуты пока загрузятся все данные

3. **Скачайте фото:**
   После завершения парсинга появится секция "📦 Скачать все фото" с двумя кнопками:

   **🧹 Скачать с удалением водяных знаков**
   - Автоматически удаляет логотипы Cian из всех фото
   - Занимает больше времени (~2-5 минут в зависимости от кол-ва фото)
   - Лучшее качество для презентаций

   **📥 Скачать оригиналы (быстрее)**
   - Просто скачивает все фото без обработки
   - Быстро (~30 секунд)
   - С водяными знаками Cian

## 📁 Структура архива

Архив содержит папки по объявлениям:

```
cian_photos_20251104_123456.zip
├── 00_Основное_объявление/
│   ├── photo_01.jpg
│   ├── photo_02.jpg
│   └── ... (до 12 фото)
├── 01_Продается_2-комн_квартира_60_м2/
│   ├── photo_01.jpg
│   ├── photo_02.jpg
│   └── ...
├── 02_Продается_2-комн_квартира_55_м2/
│   └── ...
└── ... (все похожие объявления)
```

**Организация:**
- `00_Основное_объявление/` - фото главного объявления
- `01_...`, `02_...` и т.д. - похожие объявления (нумерация + название)
- До 12 фотографий из каждого объявления
- Качество JPEG 95%

## ⚡ Технические детали

### Backend ([webapp/app_cian_style.py](webapp/app_cian_style.py:96))

**Новый endpoint:**
```python
@app.route('/download_photos', methods=['POST'])
def download_photos():
    """Скачать архив с очищенными фото"""
```

**Параметры:**
- `remove_watermarks` (bool) - удалять ли водяные знаки

**Процесс:**
1. Собирает все URL фотографий из результата парсинга
2. Создает структуру папок по объявлениям
3. Если `remove_watermarks=True`:
   - Обрабатывает каждое фото через `WatermarkRemover`
   - Удаляет водяные знаки из углов и нижней части
   - Сохраняет в высоком качестве (JPEG 95%)
4. Создает ZIP архив в памяти
5. Отправляет пользователю

**Время работы:**
- Без обработки: ~30 секунд (100 фото)
- С удалением водяных знаков: ~2-5 минут (100 фото × ~2 сек каждое)

### Frontend ([webapp/templates/index_cian_style.html](webapp/templates/index_cian_style.html:519))

**Новая секция:**
```html
<div class="download-section" id="downloadSection">
    <!-- Кнопки скачивания -->
    <!-- Прогресс-бар -->
    <!-- Информация о структуре архива -->
</div>
```

**JavaScript функция:**
```javascript
async function downloadPhotos(removeWatermarks) {
    // Показывает прогресс
    // Делает запрос к /download_photos
    // Скачивает файл
}
```

## 🎨 Качество удаления водяных знаков

**Что удаляется хорошо:**
- ✅ Логотип Cian в правом нижнем углу (100%)
- ✅ Номер телефона в правом верхнем углу (100%)
- ✅ Логотипы в левом нижнем углу (100%)

**Что удаляется частично:**
- ⚠️ Полупрозрачный текст "cian.ru" по всему изображению (~50%)

**Метод:** OpenCV inpainting (TELEA)
**Параметры:**
- Радиус inpainting: 7px
- Позиции обнаружения: bottom-right, top-right, bottom-left
- Нижняя полоса: 20% высоты изображения

## 📊 Пример использования

1. Парсите объявление: https://spb.cian.ru/sale/flat/315047063/

2. После парсинга появится кнопка "Скачать все фото"

3. Нажимаете "🧹 Скачать с удалением водяных знаков"

4. Видите прогресс:
   ```
   Удаляем водяные знаки и создаем архив...
   Это может занять несколько минут
   ```

5. Через ~2-3 минуты скачивается архив:
   ```
   cian_photos_20251104_143022.zip (25.4 MB)
   ```

6. Внутри архива:
   ```
   00_Основное_объявление/ - 12 фото
   01_Продается_2-комн_квартира_60_м2/ - 8 фото
   02_Продается_2-комн_квартира_55_м2/ - 10 фото
   ...
   10 объявлений, всего 98 фотографий
   ```

## 🔧 Настройка

### Изменить количество фото на объявление

В [webapp/app_cian_style.py](webapp/app_cian_style.py:119):
```python
# Было: первые 12 фото
for i, img_url in enumerate(last_parse_result.get('images', [])[:12]):

# Изменить на, например, все фото:
for i, img_url in enumerate(last_parse_result.get('images', [])):
```

### Изменить качество JPEG

В [webapp/app_cian_style.py](webapp/app_cian_style.py:164):
```python
# Было: 95%
cleaned_img.save(img_bytes, format='JPEG', quality=95)

# Изменить на, например, 85%:
cleaned_img.save(img_bytes, format='JPEG', quality=85)
```

### Изменить позиции удаления водяных знаков

В [webapp/app_cian_style.py](webapp/app_cian_style.py:158):
```python
# Добавить или убрать позиции
auto_detect_positions=['bottom-right', 'top-right', 'bottom-left']
```

Доступные позиции:
- `'bottom-right'` - правый нижний (логотип)
- `'top-right'` - правый верхний (телефон)
- `'bottom-left'` - левый нижний
- `'center'` - центр изображения

## 🚀 Что дальше

Возможные улучшения:

1. **Параллельная обработка фото** - ускорение в 5-10 раз
   ```python
   from concurrent.futures import ThreadPoolExecutor
   with ThreadPoolExecutor(max_workers=10) as executor:
       results = executor.map(remover.process_url, urls)
   ```

2. **Прогресс-бар в реальном времени**
   - WebSocket соединение
   - Обновление процента обработки

3. **Выбор параметров через интерфейс**
   - Количество фото на объявление
   - Качество JPEG
   - Позиции водяных знаков

4. **Предпросмотр обработанных фото**
   - Показ до/после
   - Возможность отменить обработку отдельных фото

5. **Кэширование обработанных фото**
   - Хранение результатов
   - Мгновенная повторная загрузка

## 📄 Связанные файлы

- [webapp/app_cian_style.py](webapp/app_cian_style.py) - backend с endpoint /download_photos
- [webapp/templates/index_cian_style.html](webapp/templates/index_cian_style.html) - интерфейс с кнопками
- [src/watermark_remover.py](src/watermark_remover.py) - модуль удаления водяных знаков
- [WATERMARK_REMOVAL_RESULTS.md](WATERMARK_REMOVAL_RESULTS.md) - результаты тестирования
- [WATERMARK_QUICKSTART.md](WATERMARK_QUICKSTART.md) - быстрый старт модуля

---

**Обновлено:** 2025-11-04
**Версия:** 1.0
**Автор:** Claude Code
