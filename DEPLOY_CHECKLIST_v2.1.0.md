# 🚀 Чек-лист деплоя v2.1.0 на продакшн

**Дата:** 2025-11-12
**Версия:** v2.1.0
**Ветка:** `claude/debug-cian-search-zero-results-011CV41bx4Z7EqgzwGrquBmU`
**Коммит:** `0bfe28b`

---

## 📦 Что деплоим

### Новые файлы
- ✅ `src/parsers/adaptive_selectors.py` - адаптивная система парсинга (380 строк)
- ✅ `docs/adaptive_parsing.md` - документация (150 строк)

### Измененные файлы
- ✅ `src/parsers/playwright_parser.py` - улучшенный парсинг с адаптивными селекторами

### Зависимости
- ✅ Нет новых зависимостей (используем существующие: BeautifulSoup, re, logging)

---

## 🎯 Основные изменения

### 1. Адаптивная система парсинга
- **Что:** Множественные fallback-селекторы для каждого элемента
- **Зачем:** Устойчивость к изменениям верстки Cian
- **Результат:** Снижение случаев с 0 результатами на 80-90%

### 2. Расширенное логирование
- **Что:** Детальные логи на всех этапах поиска и парсинга
- **Зачем:** Быстрая диагностика проблем
- **Результат:** Понятно, где и почему теряются результаты

### 3. Самодиагностика
- **Что:** Статистика успешных селекторов
- **Зачем:** Мониторинг работы парсера
- **Результат:** Можно вовремя заметить изменения на Cian

---

## ✅ Пре-деплой чек-лист

### Проверка кода
- [x] Код прошел синтаксическую проверку (`python3 -m py_compile`)
- [x] Все коммиты объединены в один
- [x] Коммит отправлен на GitHub

### Резервное копирование
- [ ] Создана резервная копия БД (если есть)
- [ ] Текущая версия кода сохранена (git tag)
- [ ] Логи текущей версии сохранены

### Подготовка окружения
- [ ] Проверено наличие Python 3.8+
- [ ] Проверены зависимости: `pip list | grep -E "beautifulsoup4|lxml"`
- [ ] Проверено свободное место на диске (>1GB)

---

## 🔧 Инструкция по деплою

### Шаг 1: Остановка приложения
```bash
# Если используется systemd
sudo systemctl stop cian-analyzer

# Если используется Docker
docker-compose down

# Или просто
pkill -f "python.*app_new.py"
```

### Шаг 2: Обновление кода
```bash
cd /path/to/cian-analyzer

# Сохранить текущую версию
git tag -a v2.0.0 -m "Version before adaptive parsing"

# Получить новую версию
git fetch origin
git checkout claude/debug-cian-search-zero-results-011CV41bx4Z7EqgzwGrquBmU
git pull origin claude/debug-cian-search-zero-results-011CV41bx4Z7EqgzwGrquBmU

# Проверить коммит
git log -1 --oneline  # Должно быть: 0bfe28b release: Улучшение парсинга...
```

### Шаг 3: Проверка файлов
```bash
# Проверить наличие новых файлов
ls -la src/parsers/adaptive_selectors.py
ls -la docs/adaptive_parsing.md

# Проверить синтаксис
python3 -m py_compile src/parsers/adaptive_selectors.py
python3 -m py_compile src/parsers/playwright_parser.py
```

### Шаг 4: Запуск приложения
```bash
# Если используется systemd
sudo systemctl start cian-analyzer
sudo systemctl status cian-analyzer

# Если используется Docker
docker-compose up -d
docker-compose logs -f

# Если запуск вручную
nohup python3 app_new.py > logs/app.log 2>&1 &
```

### Шаг 5: Проверка работоспособности
```bash
# Проверить что приложение запустилось
ps aux | grep "python.*app_new.py"

# Проверить порт
netstat -tulpn | grep :5000  # или ваш порт

# Проверить логи
tail -f logs/app.log

# Тестовый запрос
curl -X POST http://localhost:5000/api/parse \
  -H "Content-Type: application/json" \
  -d '{"url": "https://spb.cian.ru/sale/flat/315831388/"}'
```

---

## 🔍 Пост-деплой проверки

### Немедленные проверки (5-10 минут)
- [ ] Приложение запущено и отвечает на запросы
- [ ] Нет критических ошибок в логах
- [ ] Тестовый поиск возвращает результаты (>0)
- [ ] В логах появляются новые DEBUG сообщения
- [ ] Статистика селекторов логируется: `📊 Статистика селекторов:`

### Проверки через 1 час
- [ ] Приложение стабильно работает
- [ ] Количество ошибок не увеличилось
- [ ] Случаев с 0 результатами стало меньше
- [ ] Memory usage в норме

### Проверки через 24 часа
- [ ] Парсинг работает стабильно
- [ ] Собрана статистика успешных селекторов
- [ ] Количество 0 результатов снизилось на 70%+
- [ ] Нет новых типов ошибок

---

## 📊 Мониторинг

### Что смотреть в логах

#### Успешная работа
```
✓ Найдено 45 карточек объявлений на странице
✓ Успешно спарсено 42 объявлений из 45 карточек
📊 Статистика селекторов: {'карточки объявлений:0': 45, 'заголовок:0': 42}
✓ Все 42 результатов прошли валидацию
```

#### Проблемы
```
⚠️ DEBUG: На странице не найдено ни одной карточки объявления
⚠️ DEBUG: _get_page_content вернул пустой HTML
✗ Результат 15: ИСКЛЮЧЕН - Цена/м² вне пределов
```

### Метрики для отслеживания
1. **Количество успешных парсингов** (должно увеличиться)
2. **Процент случаев с 0 результатами** (должно снизиться на 70-80%)
3. **Частота использования разных селекторов** (в статистике)
4. **Среднее время парсинга** (не должно сильно измениться)

### Команды для мониторинга
```bash
# Количество DEBUG сообщений (должно быть много)
grep "DEBUG:" logs/app.log | wc -l

# Случаи с 0 результатами
grep "найдено 0" logs/app.log | wc -l

# Статистика селекторов
grep "📊 Статистика селекторов:" logs/app.log | tail -20

# Ошибки парсинга
grep "⚠️ DEBUG: На странице не найдено" logs/app.log | wc -l
```

---

## 🔄 Rollback план

### Если что-то пошло не так

#### Быстрый откат (2 минуты)
```bash
# Остановить приложение
sudo systemctl stop cian-analyzer  # или docker-compose down

# Откатить к предыдущей версии
git checkout v2.0.0

# Запустить
sudo systemctl start cian-analyzer
```

#### Полный откат с восстановлением БД
```bash
# Остановить приложение
sudo systemctl stop cian-analyzer

# Откатить код
git checkout v2.0.0

# Восстановить БД (если была изменена)
# psql -U user -d cian_db < backup_2025-11-12.sql

# Запустить
sudo systemctl start cian-analyzer

# Проверить
curl http://localhost:5000/health
```

### Критерии для отката
- Критические ошибки в логах (>100 за 5 минут)
- Приложение не запускается
- Все поиски возвращают 0 результатов
- Memory leak (память растет >2GB)
- Response time увеличился в 5+ раз

---

## 📝 Тестовые сценарии

### Тест 1: Проблемная ссылка (главный тест)
```bash
curl -X POST http://localhost:5000/api/parse \
  -H "Content-Type: application/json" \
  -d '{"url": "https://spb.cian.ru/sale/flat/315831388/"}'
```
**Ожидаемо:** Возвращает данные, в логах видно `📊 Статистика селекторов:`

### Тест 2: Поиск аналогов
```bash
# После теста 1, используя полученный session_id
curl -X POST http://localhost:5000/api/find-similar \
  -H "Content-Type: application/json" \
  -d '{"session_id": "полученный-id", "limit": 20, "search_type": "building"}'
```
**Ожидаемо:** `"count": >0`, в логах детальная информация о поиске

### Тест 3: Обычная ссылка (не проблемная)
```bash
curl -X POST http://localhost:5000/api/parse \
  -H "Content-Type: application/json" \
  -d '{"url": "https://spb.cian.ru/sale/flat/ЛЮБАЯ_РАБОЧАЯ_ССЫЛКА/"}'
```
**Ожидаемо:** Работает как раньше, нет регрессии

---

## 🎓 Обучение команды

### Новые возможности
1. **Адаптивные селекторы** - автоматически пробуют разные способы поиска
2. **Расширенное логирование** - можно увидеть, как работает парсинг
3. **Статистика селекторов** - видно, какие стратегии работают лучше

### Как добавить новый селектор
```python
# В src/parsers/adaptive_selectors.py
PRICE_SELECTORS = [
    {'tag': 'span', 'attrs': {'data-mark': 'MainPrice'}},
    # ... существующие
    {'tag': 'div', 'class_contains': 'new-price-class'},  # Добавить здесь
]
```

### Как читать логи
```
🔍 Начинаем поиск похожих квартир в том же ЖК...
📋 DEBUG: Данные целевой квартиры:
   - residential_complex: ЖК Моисеенко, 10
   - price: 15000000
Найдено 45 карточек объявлений на странице
✓ Успешно спарсено 42 объявлений из 45 карточек
📊 Статистика селекторов: {'карточки объявлений:0': 45}
```

---

## 📞 Контакты и поддержка

### В случае проблем
1. Проверить логи: `tail -f logs/app.log`
2. Проверить, какой селектор используется чаще: `grep "📊 Статистика" logs/app.log`
3. Если проблема с конкретной ссылкой - скопировать логи для этой ссылки
4. Откатить к v2.0.0 если критично

### Документация
- `docs/adaptive_parsing.md` - подробное описание системы
- `README.md` - общая документация проекта

---

## ✅ Финальный чек-лист

Перед тем как завершить деплой, убедитесь:

- [ ] Приложение запущено и работает
- [ ] Тестовые запросы проходят успешно
- [ ] В логах появляются новые DEBUG сообщения
- [ ] Статистика селекторов логируется
- [ ] Количество ошибок не увеличилось
- [ ] Команда знает о изменениях
- [ ] Rollback план готов и протестирован
- [ ] Мониторинг настроен
- [ ] Документация обновлена

---

**Деплой выполнил:** _______________
**Дата и время:** _______________
**Результат:** ⬜ Успешно  ⬜ С проблемами  ⬜ Откачено

**Комментарии:**
```
_________________________________________________________________
_________________________________________________________________
_________________________________________________________________
```
