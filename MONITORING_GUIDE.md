# System Monitoring & Auto-Diagnostics Guide

## 🎯 Обзор

Была внедрена полноценная система автоматического мониторинга и самодиагностики, которая:
- ✅ Автоматически проверяет здоровье системы каждые 5 минут
- ✅ Анализирует логи каждые 15 минут
- ✅ Запускает тесты каждый час
- ✅ Проводит полную диагностику каждые 2 часа
- ✅ Обнаруживает и логирует критические ошибки
- ✅ Предоставляет рекомендации по исправлению

## 🚀 Быстрый старт

### Доступ к мониторингу

1. **Веб-панель мониторинга:**
   ```
   http://your-domain.com/monitoring
   ```
   Показывает real-time состояние системы с авто-обновлением каждые 30 секунд

2. **API endpoints:**
   - `GET /api/health` - Полная проверка здоровья системы
   - `GET /api/monitoring/status` - Быстрая сводка состояния
   - `GET /api/monitoring/logs?hours=1` - Анализ логов
   - `GET /api/monitoring/test-results` - Результаты последних тестов
   - `POST /api/monitoring/diagnostic` - Запустить полную диагностику

## 📊 Компоненты системы

### 1. Health Check Service
**Файл:** `src/monitoring/health_check.py`

Проверяет состояние:
- ✅ Системные ресурсы (CPU, RAM, Disk)
- ✅ Redis/Session Storage
- ✅ Parser (CIAN)
- ✅ API endpoints
- ✅ Analyzer

**Использование:**
```python
from src.monitoring import health_service

# Запустить проверку
result = health_service.check_all()
print(result['status'])  # 'healthy', 'degraded', или 'unhealthy'

# Получить сводку за последний час
summary = health_service.get_health_summary(hours=1)
print(f"Uptime: {summary['uptime_percent']}%")
```

### 2. Test Runner
**Файл:** `src/monitoring/test_runner.py`

Автоматически запускает тесты и собирает результаты.

**Использование:**
```python
from src.monitoring import test_runner

# Запустить тесты
results = test_runner.run_all_tests()
print(f"Passed: {results['tests_passed']}/{results['tests_run']}")

# Получить результаты последнего запуска
latest = test_runner.get_latest_results()
```

### 3. Log Analyzer
**Файл:** `src/monitoring/log_analyzer.py`

Анализирует логи и обнаруживает аномалии.

**Использование:**
```python
from src.monitoring import log_analyzer

# Анализ логов за последний час
analysis = log_analyzer.analyze_recent_logs(hours=1)
print(f"Errors: {analysis['error_count']}")
print(f"Critical issues: {len(analysis['critical_issues'])}")

for issue in analysis['critical_issues']:
    print(f"- {issue['type']}: {issue['message']}")
    print(f"  Recommendation: {issue['recommendation']}")
```

### 4. Error Detector
**Файл:** `src/monitoring/error_detector.py`

Объединяет все проверки и создаёт отчёты.

**Использование:**
```python
from src.monitoring import error_detector

# Полная диагностика
diagnostic = error_detector.run_full_diagnostic()
print(f"Status: {diagnostic['overall_status']}")

# Создать текстовый отчёт
report = error_detector.create_issue_report(diagnostic)
print(report)
```

### 5. Monitoring Scheduler
**Файл:** `src/monitoring/scheduler.py`

Автоматически запускает проверки по расписанию.

**Расписание:**
- Health check: каждые 5 минут
- Log analysis: каждые 15 минут
- Tests: каждый час
- Full diagnostic: каждые 2 часа

**Управление:**
```python
from src.monitoring import monitoring_scheduler

# Запустить (автоматически запускается при старте Flask)
monitoring_scheduler.start()

# Остановить
monitoring_scheduler.stop()

# Проверить статус
status = monitoring_scheduler.get_status()
print(status)
```

## 🔧 Исправленные проблемы в продакшене

### 1. Connection Errors на Шаге 2
**Проблема:** "Ошибка соединения: Не удалось подключиться к серверу"

**Исправления:**
- ✅ Добавлен retry механизм (до 2 попыток)
- ✅ Добавлен timeout (60 секунд) на параллельный парсинг
- ✅ Улучшенная обработка ошибок с fallback на базовые данные
- ✅ Фильтрация аналогов с минимально необходимыми данными

**Код:** `app_new.py:861-943`

### 2. Missing Data на Шаге 3
**Проблема:** "Половины информации нет"

**Исправления:**
- ✅ Валидация наличия target_property перед анализом
- ✅ Проверка минимального количества аналогов (3+)
- ✅ Валидация каждого аналога (price + area обязательны)
- ✅ Пропуск невалидных аналогов с логированием
- ✅ Понятные сообщения об ошибках

**Код:** `app_new.py:1149-1230`

## 📈 Мониторинг панель

### Метрики на дашборде:
1. **Health Status** - общее состояние системы
2. **Errors (1h)** - количество ошибок за последний час
3. **Tests Passed** - количество успешных тестов
4. **Tests Failed** - количество падших тестов

### Секции:
- **Health Check Details** - детали проверки каждого компонента
- **Log Analysis** - анализ логов с критическими проблемами
- **Test Results** - результаты тестов с деталями падений

### Действия:
- **Run Full Diagnostic** - запустить полную диагностику вручную
- **Run Tests** - запустить тесты вручную

## 🔔 Автоматические алерты

Система автоматически логирует:
- 🚨 **CRITICAL** - критически высокий уровень ошибок
- ⚠️ **HIGH** - частые connection/parsing errors
- ⚠️ **MEDIUM** - validation errors

### Планы на будущее:
- Email notifications
- Slack integration
- Auto-create GitHub issues
- Webhook alerts

## 📝 Логирование

Все компоненты мониторинга логируют свою работу:
```
🏥 Running scheduled health check...
✅ Health check passed
🔍 Running scheduled full diagnostic...
⚠️ Diagnostic status: degraded, issues: 2
```

## 🧪 Тестирование мониторинга

```bash
# Импортировать модули
python3 -c "from src.monitoring import health_service, test_runner, log_analyzer, error_detector, monitoring_scheduler"

# Запустить health check
python3 -c "from src.monitoring import health_service; print(health_service.check_all())"

# Проверить статус scheduler
python3 -c "from src.monitoring import monitoring_scheduler; print(monitoring_scheduler.get_status())"
```

## 🚦 Статусы системы

- **healthy** 🟢 - всё работает нормально
- **degraded** 🟡 - есть предупреждения, но система работает
- **unhealthy** 🔴 - критические ошибки, требуется вмешательство

## 📊 API Примеры

### Проверка здоровья системы
```bash
curl http://localhost:5002/api/health
```

### Анализ логов за 3 часа
```bash
curl http://localhost:5002/api/monitoring/logs?hours=3
```

### Запустить полную диагностику
```bash
curl -X POST http://localhost:5002/api/monitoring/diagnostic
```

### Получить быструю сводку
```bash
curl http://localhost:5002/api/monitoring/status
```

## 📂 Результаты

Результаты сохраняются в:
- **Test results:** `/home/user/cian-analyzer/test_results/`
- **Error reports:** `/home/user/cian-analyzer/error_reports/`

## 🔄 Автоматический запуск

Scheduler автоматически запускается при старте Flask приложения:
```python
# app_new.py:136-138
monitoring_scheduler.start()
logger.info("Automated monitoring scheduler started")
```

## 🛠️ Конфигурация

### Изменить интервалы мониторинга
Отредактируйте `src/monitoring/scheduler.py`:
```python
# Health check каждые 5 минут
if self._should_run(self.last_health_check, minutes=5):

# Тесты каждый час
if self._should_run(self.last_test_run, minutes=60):
```

### Добавить email alerts
В `src/monitoring/scheduler.py` в методах `_handle_*`:
```python
def _handle_unhealthy_status(self, health_check):
    # Отправить email
    send_email_alert(health_check)
```

## 📚 Дополнительные ресурсы

- **Мониторинг дашборд:** `/monitoring`
- **API документация:** См. docstrings в `app_new.py`
- **Логи:** Консоль приложения или файл логов

## 🎯 Следующие шаги

1. ✅ Система мониторинга работает автоматически
2. 🔜 Добавить email/Slack уведомления
3. 🔜 Интегрировать с GitHub Issues для автоматического создания задач
4. 🔜 Добавить метрики производительности (response time, throughput)
5. 🔜 Создать Grafana/Prometheus дашборды для продвинутой визуализации

---

**Автор:** Claude Code
**Дата:** 2025-11-10
**Версия:** 1.0.0
