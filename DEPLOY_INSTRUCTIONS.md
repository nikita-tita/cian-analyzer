# 🚀 Инструкция по деплою на Railway

Проект полностью готов к деплою! Все изменения запушены и ждут развертывания.

## ✅ Готово к деплою

- ✅ `app_new.py` адаптирован для Railway (использует SimpleParser)
- ✅ `requirements.txt` содержит все необходимые зависимости
- ✅ `Procfile` и `railway.toml` настроены для gunicorn
- ✅ Все изменения закоммичены и запушены

## 🎯 Деплой через Railway Dashboard

### Шаг 1: Откройте Railway

1. Перейдите на [railway.app](https://railway.app)
2. Войдите в аккаунт (GitHub OAuth)

### Шаг 2: Создайте новый проект

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите репозиторий: **`nikita-tita/cian-analyzer`**

### Шаг 3: Настройте ветку для деплоя

**ВАЖНО**: Используйте ветку с изменениями:

```
claude/add-browser-automation-skill-011CUtW7fq3goBrhVCRLTEK1
```

Или если ветка main обновлена - используйте `main`.

В настройках проекта:
1. Перейдите в **Settings** → **Service**
2. Найдите **Branch**
3. Выберите `claude/add-browser-automation-skill-011CUtW7fq3goBrhVCRLTEK1`
4. Сохраните изменения

### Шаг 4: Railway автоматически:

- 📦 Обнаружит Python проект
- 📥 Установит зависимости из `requirements.txt`
- 🚀 Запустит через `gunicorn` (из `Procfile`)
- 🌐 Выделит публичный URL

### Шаг 5: Получите URL

После успешного деплоя:
1. Перейдите в **Settings** → **Networking**
2. Нажмите **Generate Domain**
3. Получите URL вида: `https://your-app.up.railway.app`

## 📋 Что включено в деплой

### SimpleParser вместо Playwright

Приложение использует **SimpleParser** для совместимости с serverless:
- ✅ Не требует браузера
- ✅ Использует только requests + BeautifulSoup
- ✅ Быстрая установка зависимостей
- ⚠️ Возвращает базовые/демо данные для некоторых функций

### Конфигурация Production

**Procfile:**
```
web: gunicorn app_new:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
```

**railway.toml:**
```toml
[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn app_new:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
```

### Зависимости (requirements.txt)

```
Flask>=3.0.0
pydantic>=2.5.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
python-dotenv>=1.0.0
fake-useragent>=1.4.0
tenacity>=8.2.3
numpy>=1.24.0
scipy>=1.11.0
gunicorn>=21.2.0
```

## 🔧 Альтернатива: Деплой через Railway CLI

Если предпочитаете CLI:

```bash
# 1. Установите Railway CLI
npm install -g @railway/cli

# 2. Войдите
railway login

# 3. Создайте проект
railway init

# 4. Свяжите с репозиторием
railway link

# 5. Задеплойте
railway up
```

## 🎨 Что работает после деплоя

✅ **Главная страница** - веб-интерфейс анализатора
✅ **API `/api/parse`** - парсинг URL недвижимости
✅ **API `/api/find-similar`** - поиск аналогов (демо-данные)
✅ **API `/api/analyze`** - сравнительный анализ
✅ **Статические файлы** - CSS, JS, изображения

## ⚠️ Ограничения SimpleParser

SimpleParser в production режиме:
- 📊 Извлекает базовые данные из URL (цена, площадь, комнаты)
- 🎭 Использует значения по умолчанию для некоторых полей
- 🔄 Возвращает демо-данные для аналогов
- 💡 Добавляет предупреждение `_parser_type: 'simple'`

**Для полноценного парсинга используйте локальную версию с Playwright.**

## 📊 Мониторинг

После деплоя в Railway Dashboard:
- 📈 **Metrics** - использование CPU/RAM
- 📝 **Logs** - логи приложения в реальном времени
- 🔄 **Deployments** - история деплоев
- ⚙️ **Settings** - настройки и переменные окружения

## 🐛 Troubleshooting

### Приложение не запускается

Проверьте логи:
```
railway logs
```

Возможные причины:
- ❌ Не установились зависимости → проверьте requirements.txt
- ❌ Порт не связан → убедитесь что используется `$PORT` в gunicorn
- ❌ Ошибки импорта → проверьте структуру проекта

### 502 Bad Gateway

- Проверьте, что gunicorn запущен: `ps aux | grep gunicorn`
- Убедитесь, что приложение слушает `0.0.0.0:$PORT`
- Проверьте логи на Python ошибки

### Таймауты

Увеличьте таймаут в `Procfile`:
```
--timeout 300
```

## 🔗 Полезные ссылки

- [Railway Docs](https://docs.railway.app/)
- [Railway Python Guide](https://docs.railway.app/guides/python)
- [Gunicorn Docs](https://docs.gunicorn.org/)

---

## 🎉 Готово!

После деплоя откройте ваш URL и наслаждайтесь работающим приложением!

```
https://your-app.up.railway.app
```

**Все файлы готовы, просто нажмите Deploy! 🚀**
