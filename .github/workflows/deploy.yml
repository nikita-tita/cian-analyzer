name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  test-before-deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov

    - name: Run tests
      run: |
        python -m pytest tests/ \
          --ignore=tests/test_e2e_full_flow.py \
          -v \
          --cov=src \
          --cov-fail-under=60

    - name: Notify on failure
      if: failure()
      run: |
        echo "❌ Tests failed! Deploy aborted."
        exit 1

  deploy:
    runs-on: ubuntu-latest
    needs: test-before-deploy

    steps:
    - name: Deploy to server
      run: |
        echo "✅ Tests passed! Deploying to production..."
        # SSH deploy команды будут здесь
        # Сейчас деплой делается вручную

    - name: Run E2E smoke tests
      run: |
        echo "🧪 Running smoke tests on production..."
        # Здесь можно запустить критические E2E тесты
