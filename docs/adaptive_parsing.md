# Адаптивный парсинг для устойчивости к изменениям верстки

## Проблема

Сайты, такие как Cian, регулярно меняют HTML-структуру и CSS-классы, что приводит к поломке парсеров, которые зависят от конкретных селекторов.

## Решение: Адаптивные селекторы

Мы реализовали систему **множественных fallback-стратегий** для поиска элементов на странице.

### Основные подходы

1. **Множественные селекторы с приоритетами**
   ```python
   TITLE_SELECTORS = [
       # Приоритет 1: Официальный data-атрибут
       {'tag': 'span', 'attrs': {'data-mark': 'OfferTitle'}},

       # Приоритет 2: Стандартные теги
       {'tag': 'h3'},
       {'tag': 'a', 'attrs': {'data-name': 'LinkArea'}},

       # Приоритет 3: По частичному совпадению класса
       {'tag': 'div', 'class_contains': 'title'},
   ]
   ```

2. **Поиск по содержимому текста**
   ```python
   {'tag': True, 'text_contains': '₽'}  # Найдет элемент с символом рубля
   {'tag': True, 'text_contains': 'метро'}  # Найдет элемент с текстом "метро"
   ```

3. **Регулярные выражения для атрибутов**
   ```python
   {'tag': 'div', 'attr_regex': {'attr': 'data-mark', 'pattern': r'offer'}}
   ```

4. **Контент-ориентированный парсинг**
   - Извлекаем данные из текста с помощью regex
   - Не зависим от конкретной структуры HTML
   ```python
   extract_rooms_from_text("2-комн. квартира, 85 м²")  # → "2"
   extract_floor_from_text("4/9 этаж")  # → 4
   ```

## Преимущества

### 1. Устойчивость к изменениям
- Если один селектор ломается → автоматически пробуются следующие
- Поддержка старых и новых форматов одновременно

### 2. Самодиагностика
- Логирование, какой селектор сработал
- Статистика успешных селекторов
```
📊 Статистика селекторов: {'карточки объявлений:0': 150, 'заголовок:0': 145, 'цена:1': 150}
```

### 3. Легкое добавление новых вариантов
Достаточно добавить селектор в список:
```python
CARD_SELECTORS = [
    {'tag': 'article', 'attrs': {'data-name': 'CardComponent'}},
    {'tag': 'article', 'class_contains': 'card'},
    # ... добавить новый вариант здесь
]
```

## Примеры использования

### Парсинг карточек объявлений

```python
from adaptive_selectors import AdaptiveSelector, CARD_SELECTORS

selector = AdaptiveSelector(soup)
cards = selector.find_elements(CARD_SELECTORS, "карточки объявлений")
# Пробует 5 разных способов найти карточки
```

### Извлечение цены

```python
from adaptive_selectors import PRICE_SELECTORS

price_elem = selector.find_element(PRICE_SELECTORS, "цена")
# Пробует: data-mark, class с "price", itemprop, текст с "₽"
```

### Извлечение количества комнат

```python
from adaptive_selectors import extract_rooms_from_text

rooms = extract_rooms_from_text("2-комн. квартира")  # → "2"
rooms = extract_rooms_from_text("студия 35 м²")      # → "студия"
rooms = extract_rooms_from_text("3к квартира")       # → "3"
```

## Файлы

- `src/parsers/adaptive_selectors.py` - модуль с адаптивными селекторами
- `src/parsers/playwright_parser.py` - интеграция в основной парсер

## Мониторинг

Система логирует:
- Какие селекторы срабатывают чаще всего
- Когда элементы не найдены ни одним селектором
- Статистику успешных парсингов

Это помогает:
- Вовремя заметить изменения на сайте
- Понять, какие селекторы устарели
- Оптимизировать порядок селекторов (самые успешные - первыми)

## Будущие улучшения

1. **AI-парсинг** - использовать LLM для извлечения данных из HTML (для сложных случаев)
2. **Автоматическое обучение** - анализ статистики и переупорядочивание селекторов
3. **Алерты** - уведомления, когда селекторы перестают работать
4. **A/B тестирование** - сравнение разных стратегий парсинга
