# 📰 Блог Housler - Инструкция

Система автоматического парсинга и рерайтинга статей для блога о недвижимости.

## 🎯 Возможности

- ✅ Парсинг статей с Cian Magazine (СПб и Москва)
- ✅ Автоматический рерайтинг через Яндекс GPT
- ✅ Красивая HTML разметка статей
- ✅ Система категорий и тегов
- ✅ Счетчики просмотров и лайков
- ✅ Избранные статьи
- ✅ Похожие статьи
- ✅ SEO-оптимизация (meta description, Open Graph)
- ✅ Адаптивный дизайн

## 🚀 Быстрый старт

### 1. Настройка Яндекс GPT (опционально)

Для автоматического рерайтинга нужно настроить Яндекс GPT:

```bash
# Получите API ключ в Яндекс Cloud Console:
# https://console.cloud.yandex.ru/

# Установите переменные окружения
export YANDEX_API_KEY=your_api_key_here
export YANDEX_FOLDER_ID=your_folder_id_here
```

**Важно:** Без Яндекс GPT статьи будут сохраняться с базовой обработкой (очистка HTML и простое форматирование).

### 2. Генерация первых статей

Запустите скрипт для создания первых 5 статей:

```bash
./generate_blog_articles.py
```

Или с параметрами:

```bash
# Создать 10 статей из Москвы
./generate_blog_articles.py --count 10 --region msk

# Создать 3 статьи из СПб
./generate_blog_articles.py --count 3 --region spb
```

### 3. Запуск сервера

```bash
# Запустите Flask приложение
python app_new.py

# Откройте в браузере
open http://localhost:5000/blog
```

## 📖 Использование

### Просмотр блога

- **Список статей:** http://localhost:5000/blog
- **Фильтр по категории:** http://localhost:5000/blog?category=Ипотека
- **Пагинация:** http://localhost:5000/blog?page=2
- **Отдельная статья:** http://localhost:5000/blog/[slug]

### API Endpoints

#### Получить список статей
```bash
GET /api/blog/articles?page=1&per_page=12&category=Недвижимость
```

#### Получить статью по ID
```bash
GET /api/blog/articles/{article_id}
```

#### Лайкнуть статью
```bash
POST /api/blog/articles/{article_id}/like
```

#### Парсить и создать статью
```bash
POST /api/blog/parse-and-create
Content-Type: application/json

{
  "url": "https://www.cian.ru/magazine/kak-kupit-kvartiru/",
  "featured": false
}
```

#### Получить категории
```bash
GET /api/blog/categories
```

## 🔧 Ручное управление

### Создание статьи через Python

```python
from src.blog import BlogStorage, YandexGPTRewriter
from src.parsers.cian_magazine_parser import CianMagazineParser
from src.models.blog import BlogArticle

# Инициализация
parser = CianMagazineParser()
rewriter = YandexGPTRewriter()
storage = BlogStorage()

# Парсинг статьи
url = "https://www.cian.ru/magazine/kak-prodat-kvartiru/"
article_data = parser.parse_article_content(url)

# Рерайт
rewritten = rewriter.rewrite_article(
    original_content=article_data['content'],
    title=article_data['title']
)

# Создание
article = BlogArticle(
    title=article_data['title'],
    original_content=article_data['content'],
    rewritten_content=rewritten,
    cover_image=article_data.get('cover_image'),
    source_url=url,
    category="Недвижимость",
    status='published'
)

saved = storage.create(article)
print(f"Создана статья: /blog/{saved.slug}")
```

### Работа с хранилищем

```python
from src.blog import BlogStorage

storage = BlogStorage()

# Получить все опубликованные статьи
response = storage.get_all(status='published', limit=10)
print(f"Найдено {response.total} статей")

# Получить статью по slug
article = storage.get_by_slug('kak-kupit-kvartiru-v-spb')

# Обновить статью
storage.update(article.id, {
    'featured': True,
    'category': 'Покупка'
})

# Удалить статью
storage.delete(article.id)

# Увеличить просмотры
storage.increment_views(article.id)

# Получить избранные
featured = storage.get_featured(limit=3)

# Получить последние
latest = storage.get_latest(limit=5)
```

## 📁 Структура проекта

```
cian-analyzer/
├── src/
│   ├── blog/
│   │   ├── __init__.py
│   │   ├── blog_storage.py         # Хранилище статей (JSON)
│   │   └── yandex_gpt_rewriter.py  # Рерайтер через Яндекс GPT
│   ├── models/
│   │   └── blog.py                 # Модели данных (Pydantic)
│   └── parsers/
│       └── cian_magazine_parser.py # Парсер Cian Magazine
├── templates/
│   └── blog/
│       ├── list.html               # Список статей
│       └── article.html            # Страница статьи
├── static/
│   └── css/
│       └── blog.css                # Стили блога
├── data/
│   └── blog_articles.json          # База данных статей
└── generate_blog_articles.py       # Скрипт генерации
```

## 🎨 Категории

Доступные категории статей:
- Недвижимость (по умолчанию)
- Ипотека
- Новостройки
- Советы
- Рынок
- Инвестиции
- Законодательство

## 🔍 SEO

Каждая статья автоматически получает:
- **Title:** Заголовок статьи
- **Meta Description:** Первые 160 символов контента
- **Slug:** URL-friendly версия заголовка (транслитерация)
- **Open Graph:** Теги для красивого шеринга в соцсетях
- **Canonical URL:** Уникальный URL статьи

## 🎯 Расширение функционала

### Добавление новых источников

Создайте новый парсер по аналогии с `CianMagazineParser`:

```python
class MySourceParser:
    def parse_article_list(self, limit=10):
        # Парсинг списка статей
        pass

    def parse_article_content(self, url):
        # Парсинг полного контента
        pass
```

### Генерация изображений

В будущем можно добавить генерацию обложек через:
- DALL-E / Midjourney API
- Stable Diffusion
- Yandex Art

### Автоматизация

Настройте cron для автоматической генерации статей:

```bash
# Каждый день в 10:00 создавать 3 новые статьи
0 10 * * * cd /path/to/cian-analyzer && ./generate_blog_articles.py --count 3
```

## ⚠️ Важные замечания

1. **Уникальность контента:** Яндекс GPT делает рерайт, но рекомендуется проверять уникальность перед публикацией
2. **Авторские права:** Убедитесь что у вас есть право использовать контент источников
3. **Лимиты API:** У Яндекс GPT есть лимиты на количество запросов
4. **Хранилище:** Текущая версия использует JSON файл. Для продакшена рекомендуется PostgreSQL или MongoDB

## 🐛 Решение проблем

### Ошибка "Blog not available"

Проверьте логи app_new.py. Возможные причины:
- Не установлены зависимости (Playwright, BeautifulSoup4)
- Ошибка импорта модулей

### Статьи не парсятся

1. Проверьте что Playwright установлен:
   ```bash
   pip install playwright
   playwright install chromium
   ```

2. Попробуйте с headless=False для отладки:
   ```python
   parser = CianMagazineParser(headless=False)
   ```

### Яндекс GPT не работает

1. Проверьте переменные окружения:
   ```bash
   echo $YANDEX_API_KEY
   echo $YANDEX_FOLDER_ID
   ```

2. Проверьте квоты в Yandex Cloud Console

3. Если не настроен - статьи будут создаваться с базовой обработкой

## 📞 Поддержка

При возникновении вопросов или проблем:
1. Проверьте логи приложения
2. Убедитесь что все зависимости установлены
3. Проверьте права на запись в директорию `data/`

## 🎉 Готово!

Теперь у вас есть полностью функциональный блог с автоматическим парсингом и рерайтингом статей!

**Следующие шаги:**
1. Сгенерируйте первые статьи: `./generate_blog_articles.py`
2. Откройте блог: http://localhost:5000/blog
3. Настройте автоматизацию через cron
4. Добавьте больше источников для парсинга
