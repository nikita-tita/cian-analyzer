# 🤖 Telegram Bot Integration для Housler

## 📋 Что это?

Интеграция Telegram бота позволяет пользователям получать PDF отчеты о недвижимости прямо в Telegram через безопасные одноразовые ссылки.

## 🔐 Безопасность

### ✅ Реализованные меры безопасности:

1. **Одноразовые токены**
   - Каждый токен можно использовать только один раз
   - После использования токен помечается как `used` и больше не принимается

2. **Ограниченное время жизни**
   - Токены действительны только 1 час (3600 секунд)
   - После истечения токен автоматически удаляется из Redis

3. **Redis хранилище**
   - Токены хранятся в Redis с автоматическим TTL (time-to-live)
   - В случае недоступности Redis используется fallback на in-memory с теми же ограничениями
   - Prefix `telegram_token:` для изоляции от других данных

4. **Защита от replay attacks**
   - После использования токен хранится еще 5 минут с пометкой `used=true`
   - Повторные запросы с тем же токеном отклоняются с ошибкой 410

5. **Конфиденциальность**
   - Токен бота хранится в `.env` (не в коде)
   - `.env` файл в `.gitignore` - никогда не попадет в git

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Основные зависимости для бота
pip install python-telegram-bot python-dotenv requests

# Redis (опционально, но рекомендуется для production)
# macOS
brew install redis
brew services start redis

# Ubuntu/Debian
sudo apt-get install redis-server
sudo systemctl start redis
```

### 2. Настройка переменных окружения

Файл `.env` уже содержит необходимые настройки:

```env
# Redis (уже включен)
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379

# Telegram Bot (уже настроен)
TELEGRAM_BOT_TOKEN=7737709820:AAHqZzJewfGLkRLKs9HAK3l5yGRpcVtjcY0
TELEGRAM_BOT_USERNAME=housler_bot
```

**⚠️ ВАЖНО:** Никогда не коммитьте `.env` файл в git!

### 3. Запуск бота

```bash
# Из корня проекта
python telegram_bot.py
```

Вы должны увидеть:
```
INFO - Запуск Housler Telegram бота...
INFO - ✓ Бот запущен и готов к работе
```

### 4. Запуск основного приложения

```bash
# В отдельном терминале
python app_new.py
```

## 📱 Как это работает

### Для пользователей:

1. Пользователь создает анализ на housler.ru
2. На шаге 3 нажимает кнопку "Поделиться"
3. Выбирает "Получить в Telegram"
4. Выбирает один из вариантов:
   - **Открыть в Telegram** - прямой переход по deep link
   - **QR-код** - сканирование с телефона

5. Бот автоматически отправляет:
   - Краткое описание объекта
   - PDF файл с полным отчетом
   - Ссылку на веб-версию отчета

### Технический flow:

```
1. Frontend → POST /api/telegram/generate-link
   ↓
2. Backend создает токен, сохраняет в Redis (TTL: 1 час)
   ↓
3. Frontend получает: https://t.me/housler_bot?start=TOKEN
   ↓
4. Пользователь открывает ссылку → Telegram запускает бота
   ↓
5. Бот → GET /api/telegram/report/TOKEN
   ↓
6. Backend валидирует токен, помечает как использованный
   ↓
7. Backend возвращает: description + pdf_url + web_url
   ↓
8. Бот скачивает PDF и отправляет пользователю
```

## 🔧 API Endpoints

### POST `/api/telegram/generate-link`

Создает одноразовый токен для доступа к отчету.

**Request:**
```json
{
  "session_id": "uuid-здесь"
}
```

**Response (200):**
```json
{
  "status": "success",
  "bot_link": "https://t.me/housler_bot?start=abc123...",
  "token": "abc123...",
  "expires_in": 3600
}
```

**Errors:**
- `400` - session_id не указан
- `404` - Сессия не найдена
- `400` - Анализ не выполнен

### GET `/api/telegram/report/<token>`

Получает информацию об отчете по токену (для бота).

**Response (200):**
```json
{
  "status": "success",
  "session_id": "uuid",
  "property_address": "Санкт-Петербург, ул. Ленина, 10",
  "description": "📊 Анализ недвижимости\n\n🏠 Объект: 2-комн., 65 м²\n...",
  "pdf_url": "https://housler.ru/api/export-report/SESSION_ID",
  "web_url": "https://housler.ru/wizard?session=SESSION_ID"
}
```

**Errors:**
- `404` - Токен не найден
- `410` - Токен истек
- `410` - Токен уже использован

## 🛡️ Production Deployment

### Railway / Heroku

1. **Добавьте Redis addon:**
```bash
# Railway автоматически предоставит REDIS_URL
# Обновите .env для использования $REDIS_URL
```

2. **Настройте переменные окружения:**
```bash
REDIS_ENABLED=true
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_BOT_USERNAME=housler_bot
API_BASE_URL=https://housler.ru
```

3. **Запустите бота как отдельный процесс:**
```yaml
# Procfile
web: gunicorn app_new:app
bot: python telegram_bot.py
```

### Docker

```dockerfile
# Dockerfile для бота
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY telegram_bot.py .
COPY .env .

CMD ["python", "telegram_bot.py"]
```

```yaml
# docker-compose.yml
version: '3.8'
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis

  bot:
    build: .
    command: python telegram_bot.py
    environment:
      - REDIS_HOST=redis
      - API_BASE_URL=http://web:5000
    depends_on:
      - redis
      - web
```

## 🧪 Тестирование

### Ручное тестирование:

1. Создайте отчет на localhost:5000
2. На шаге 3 откройте DevTools → Network
3. Нажмите "Поделиться" → "Получить в Telegram"
4. Скопируйте токен из запроса
5. Протестируйте в Telegram или через curl:

```bash
# Проверка генерации токена
curl -X POST http://localhost:5000/api/telegram/generate-link \
  -H "Content-Type: application/json" \
  -d '{"session_id": "YOUR_SESSION_ID"}'

# Проверка получения отчета
curl http://localhost:5000/api/telegram/report/YOUR_TOKEN
```

### Проверка безопасности:

```bash
# 1. Токен работает только один раз
curl http://localhost:5000/api/telegram/report/TOKEN
# → 200 OK

curl http://localhost:5000/api/telegram/report/TOKEN
# → 410 Gone (already used)

# 2. Истекшие токены
# Подождите >1 часа или измените expires_in в коде
curl http://localhost:5000/api/telegram/report/OLD_TOKEN
# → 410 Gone (expired)

# 3. Несуществующие токены
curl http://localhost:5000/api/telegram/report/fake-token
# → 404 Not Found
```

## 📊 Мониторинг

### Redis мониторинг:

```bash
# Подключение к Redis
redis-cli

# Просмотр всех токенов
KEYS telegram_token:*

# Проверка TTL токена
TTL telegram_token:abc123...

# Просмотр данных токена
GET telegram_token:abc123...
```

### Логи:

Backend логирует все операции с токенами:
```
[TelegramTokenStorage] Токен abc123... сохранен в Redis (TTL: 3600s)
[TelegramTokenStorage] Токен abc123... помечен как использованный
```

## ❓ FAQ

**Q: Что если Redis недоступен?**
A: Автоматический fallback на in-memory хранилище с теми же ограничениями безопасности.

**Q: Можно ли увеличить время жизни токена?**
A: Да, измените `expires_in=3600` на нужное значение в `generate_telegram_link()`.

**Q: Как защититься от брутфорса токенов?**
A: UUID4 токены имеют 122 бита энтропии (2^122 вариантов), что делает брутфорс невозможным.

**Q: Что делать при утечке токена бота?**
A: 1) Получите новый токен через @BotFather, 2) Обновите `.env`, 3) Перезапустите бота.

## 🤝 Поддержка

- Telegram: @nickkita
- GitHub Issues: https://github.com/anthropics/housler/issues
