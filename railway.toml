[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn app_new:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-level debug"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[variables]
FLASK_ENV = "production"
PYTHONUNBUFFERED = "1"
