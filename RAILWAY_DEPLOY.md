# Railway.app Deployment Guide - ПОЛНОФУНКЦИОНАЛЬНАЯ ВЕРСИЯ

🎉 **Railway = полная версия со всеми возможностями!**

## 🚀 Почему Railway?

| Функция | Railway ✅ | Vercel ❌ |
|---------|-----------|-----------|
| **Playwright** | ✅ Работает | ❌ Слишком большой |
| **Async парсинг** | ✅ 5x быстрее | ❌ Не поддерживается |
| **Redis** | ✅ Встроенный | ⚠️ Нужен внешний |
| **PostgreSQL** | ✅ Встроенный | ⚠️ Нужен внешний |
| **Docker** | ✅ Полная поддержка | ❌ Нет |
| **Timeout** | ✅ Без лимита | ⚠️ 10-60s |
| **Цена** | ✅ $5 бесплатно | $0 / $20 |

---

## 📋 Что получаете на Railway

✅ **Playwright** - полный парсинг с JavaScript  
✅ **Async парсинг** - 5x ускорение (20 объектов за 12 секунд)  
✅ **Redis** - встроенный, автоматическая настройка  
✅ **PostgreSQL** - встроенный, автоматическая настройка  
✅ **Все возможности v2.0** - без ограничений  

---

## 🚀 Деплой за 5 минут

### Шаг 1: Регистрация на Railway

1. Перейдите на **[railway.app](https://railway.app)**
2. Нажмите **"Start a New Project"**
3. Войдите через GitHub

### Шаг 2: Создание проекта

1. Нажмите **"Deploy from GitHub repo"**
2. Выберите **`nikita-tita/cian-analyzer`**
3. Выберите ветку: **`claude/review-project-011CUrTS5jNGPrP8p61s7prx`**

### Шаг 3: Добавление сервисов

#### 3.1 Добавить Redis

1. В вашем проекте нажмите **"+ New"**
2. Выберите **"Database" → "Add Redis"**
3. Railway автоматически создаст переменные:
   - `REDIS_URL`
   - `REDIS_PRIVATE_URL`

#### 3.2 Добавить PostgreSQL

1. В проекте нажмите **"+ New"**
2. Выберите **"Database" → "Add PostgreSQL"**
3. Railway автоматически создаст переменные:
   - `DATABASE_URL`
   - `DATABASE_PRIVATE_URL`

### Шаг 4: Настройка переменных окружения

В вашем сервисе (app) перейдите в **Variables** и добавьте:

```bash
# Эти переменные Railway создаст автоматически из Redis и PostgreSQL
# Вам нужно добавить только эти:

SECRET_KEY=your-super-secret-key-here
LOG_LEVEL=INFO
CACHE_ENABLED=true
ASYNC_MAX_CONCURRENT=5
```

Railway автоматически свяжет `REDIS_URL` и `DATABASE_URL` из сервисов!

### Шаг 5: Deploy!

Railway автоматически:
1. ✅ Билдит Docker образ
2. ✅ Устанавливает Playwright
3. ✅ Подключает Redis и PostgreSQL
4. ✅ Деплоит приложение

⏱️ **Первый деплой: 5-10 минут**

---

## 🔍 Проверка деплоя

После успешного деплоя Railway выдаст URL:

```
https://your-app.railway.app
```

### Проверьте endpoints:

```bash
# Health check
curl https://your-app.railway.app/health

# Metrics
curl https://your-app.railway.app/api/metrics

# Info
curl https://your-app.railway.app/api/info
```

Ожидаемый ответ `/health`:

```json
{
  "status": "healthy",
  "timestamp": "2025-01-15T12:00:00",
  "services": {
    "redis": true,
    "postgres": true,
    "cache": true
  }
}
```

---

## 📊 Производительность

### Railway

- ⏱️ **Timeout**: Без лимита
- 💾 **Memory**: 8GB (можно увеличить)
- 🚀 **Парсинг**: 20 объектов за ~12 секунд (async)
- 💰 **Цена**: $5 бесплатных кредитов/месяц

### Холодный старт

- **Первый запрос**: 10-30 секунд (загрузка Playwright)
- **Последующие**: <1 секунда

---

## 💰 Цены Railway

### Hobby Plan (Trial - $5 бесплатно)

- ✅ $5 бесплатных кредитов при регистрации
- ✅ Хватит на ~100-500 часов работы
- ✅ Все функции доступны
- ✅ Можно подключить кредитную карту для автопополнения

### Developer Plan ($5+/месяц)

- ✅ $5 включены
- ✅ Потом pay-as-you-go (~$0.002/минута)
- ✅ Неограниченные сервисы

**Оценка затрат для Cian Analyzer:**

- Постоянная работа: ~$10-20/месяц
- Периодическое использование: $5-10/месяц

---

## ⚙️ Environment Variables (полный список)

Railway автоматически создаст эти из сервисов:

```bash
# Автоматически из Redis сервиса
REDIS_URL=redis://...
REDIS_PRIVATE_URL=redis://...

# Автоматически из PostgreSQL сервиса
DATABASE_URL=postgresql://...
DATABASE_PRIVATE_URL=postgresql://...
```

Вам нужно добавить вручную:

```bash
# Flask
SECRET_KEY=your-very-secure-secret-key

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/cian_analyzer.log

# Cache
CACHE_ENABLED=true
CACHE_MEMORY_MAX_SIZE=100

# Async Parser
ASYNC_MAX_CONCURRENT=5
ASYNC_TIMEOUT=30000
ASYNC_RETRY_ATTEMPTS=3

# Flask Environment
FLASK_ENV=production
FLASK_DEBUG=false
```

---

## 🔧 Продвинутая настройка

### Custom Domain

1. В Railway проекте → Settings → Domains
2. Нажмите "Add Custom Domain"
3. Введите ваш домен: `analyzer.yourdomain.com`
4. Настройте DNS CNAME запись

### SSL/TLS

✅ Railway автоматически выдаёт Let's Encrypt сертификаты

### Логи

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите
railway login

# Смотрите логи
railway logs
```

### Мониторинг

Railway Dashboard показывает:
- 📊 CPU usage
- 💾 Memory usage
- 📈 Network traffic
- 🔥 Deploy history

---

## 🐛 Troubleshooting

### Ошибка: "Out of memory"

**Проблема:** Playwright требует много памяти

**Решение:**

1. В Railway → Settings → Resources
2. Увеличьте Memory до 2GB или больше

### Ошибка: "Redis connection failed"

**Проблема:** Неправильные переменные окружения

**Решение:**

1. Проверьте что Redis сервис запущен
2. Убедитесь что переменные `REDIS_URL` созданы автоматически
3. Перезапустите deployment

### Медленный холодный старт

**Проблема:** Playwright загружается долго

**Решение:**

- Это нормально для первого запроса (~20-30 секунд)
- Последующие запросы будут быстрыми
- Можно настроить keep-alive пинги

### Playwright браузер не запускается

**Проблема:** Отсутствуют системные зависимости

**Решение:**

- Dockerfile уже содержит все нужные зависимости
- Если проблема осталась, проверьте логи: `railway logs`

---

## 🔄 CI/CD

Railway автоматически деплоит при:

- ✅ Push в ветку `claude/review-project-011CUrTS5jNGPrP8p61s7prx`
- ✅ Merge Pull Request
- ✅ Новый коммит

### Настройка веток

В Railway → Settings → Deploy:

```
Source Repo: nikita-tita/cian-analyzer
Branch: claude/review-project-011CUrTS5jNGPrP8p61s7prx
```

---

## 📈 Сравнение с Vercel

| Параметр | Railway ✅ | Vercel |
|----------|-----------|--------|
| **Full Features** | ✅ Все | ⚠️ Ограничено |
| **Playwright** | ✅ | ❌ |
| **Async Parsing** | ✅ 5x faster | ❌ |
| **Timeout** | ✅ Безлимит | ⚠️ 10-60s |
| **Setup** | ✅ Проще | ⚠️ Нужны внешние сервисы |
| **Цена** | ~$10/мес | $0 / $20/мес |

---

## ✅ Чеклист деплоя

- [ ] Аккаунт на Railway создан
- [ ] Проект создан из GitHub
- [ ] Redis сервис добавлен
- [ ] PostgreSQL сервис добавлен
- [ ] Environment variables настроены
- [ ] Первый deploy успешен (зелёный статус)
- [ ] Health check работает: `/health`
- [ ] Metrics доступны: `/api/metrics`
- [ ] Главная страница открывается
- [ ] Парсинг работает (протестировать)

---

## 🎉 Готово!

После успешного деплоя у вас будет:

✅ **Полнофункциональный** Cian Analyzer  
✅ **Все возможности v2.0** без ограничений  
✅ **Playwright + Async парсинг** = максимальная скорость  
✅ **Redis + PostgreSQL** = персистентность данных  
✅ **Автоматические деплои** при push  

**URL вашего приложения:**

```
https://your-app.railway.app
```

---

## 🆘 Нужна помощь?

- **Railway Docs:** https://docs.railway.app/
- **Railway Discord:** https://discord.gg/railway
- **Логи:** `railway logs --follow`

---

**Версия:** 2.0.0  
**Дата:** 2025-01-15  
**Статус:** ✅ Production Ready
