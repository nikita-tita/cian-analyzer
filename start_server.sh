#!/bin/bash

# Скрипт для запуска веб-приложения Cian Analyzer
# Использование: ./start_server.sh

echo "================================"
echo "🚀 Запуск Cian Analyzer"
echo "================================"
echo ""

# Переходим в корневую директорию проекта
cd "$(dirname "$0")"

# Устанавливаем PYTHONPATH
export PYTHONPATH="$(pwd):$PYTHONPATH"

# Проверяем установлены ли зависимости
if ! python3 -c "import flask" 2>/dev/null; then
    echo "⚠️  Flask не установлен. Устанавливаем зависимости..."
    pip3 install -r requirements.txt --break-system-packages --quiet
    echo "✅ Зависимости установлены"
fi

# Запускаем сервер
echo ""
echo "Запускаем Flask сервер..."
echo ""

python3 webapp/app_main.py
